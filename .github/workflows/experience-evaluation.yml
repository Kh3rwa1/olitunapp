name: Experience evaluation

on:
  pull_request:
    paths:
      - 'lib/core/accessibility/**'
      - 'lib/core/motion/**'
      - 'lib/core/ads/**'
      - 'lib/shared/repositories/**'
      - 'lib/features/rhymes/presentation/providers/**'
      - 'test/core/ads/**'
      - 'test/shared/repositories/**'
      - 'test/features/rhymes/**'
      - 'assets/seed/**'
      - 'lib/shared/widgets/buttons/pressable_button.dart'
      - 'lib/shared/widgets/bento_grid.dart'
      - 'lib/features/lessons/**'
      - 'lib/features/profile/presentation/providers/user_stats_provider.dart'
      - 'lib/main.dart'
      - 'lib/core/auth/session_persistence.dart'
      - 'lib/shared/providers/content_providers.dart'
      - 'lib/shared/repositories/content_repository.dart'
      - 'lib/features/home/presentation/home_screen.dart'
      - 'test/core/auth/session_persistence_test.dart'
      - 'test/core/auth/account_deletion_regression_test.dart'
      - 'test/core/auth/appwrite_auth_service_test.dart'
      - 'test/shared/providers/content_list_provider_test.dart'
      - 'test/shared/providers/content_list_cache_first_test.dart'
      - 'test/shared/repositories/content_repository_cached_list_test.dart'
      - 'test/features/home/home_screen_test.dart'
      - 'test/core/accessibility/**'
      - 'test/core/motion/**'
      - 'test/shared/widgets/experience_controls_test.dart'
      - 'test/features/lessons/lessons_experience_test.dart'
      - 'test/features/profile/user_stats_sync_lifecycle_test.dart'
      - 'integration_test/experience_performance_test.dart'
      - 'tool/experience_frame_budget*'
      - 'tool/report_experience_checks.py'
      - '.github/workflows/experience-evaluation.yml'
  push:
    branches: [main]
    paths:
      - 'tool/experience_frame_budget*'
      - '.github/workflows/experience-evaluation.yml'
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: experience-${{ github.ref }}
  cancel-in-progress: true

jobs:
  measurement-validator:
    name: Performance measurement validator tests
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2
        with:
          persist-credentials: false
      - uses: actions/setup-node@94196ee1d15439c1b6651cd87ef14e88ec435966 # v4.1.0
        with:
          node-version: 22
      - run: node --test tool/experience_frame_budget.test.mjs

  targeted-flutter-diagnostics:
    name: Experience Flutter diagnostics
    if: github.event_name == 'pull_request' && github.event.pull_request.head.repo.full_name == github.repository
    runs-on: ubuntu-latest
    timeout-minutes: 20
    permissions:
      contents: read
      pull-requests: write
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2
        with:
          ref: ${{ github.event.pull_request.head.sha }}
          persist-credentials: false
      - uses: subosito/flutter-action@4cab68ce0f1c7c924f688bff4792e044f1aeb30c # v2.18.0
        with:
          channel: stable
          cache: true
      - run: flutter pub get
      - name: Collect fast targeted diagnostics
        continue-on-error: true
        env:
          TESTED_HEAD: ${{ github.event.pull_request.head.sha }}
        run: python3 tool/report_experience_checks.py
      - name: Publish early diagnostic report on this PR
        if: always() && hashFiles('build/experience-diagnostics/comment.json') != ''
        env:
          GH_TOKEN: ${{ github.token }}
          REPOSITORY: ${{ github.repository }}
          PR_NUMBER: ${{ github.event.pull_request.number }}
        run: gh api --method POST "repos/$REPOSITORY/issues/$PR_NUMBER/comments" --input build/experience-diagnostics/comment.json
      - name: Collect full Flutter diagnostics
        env:
          TESTED_HEAD: ${{ github.event.pull_request.head.sha }}
        run: |
          rm -f build/experience-diagnostics/comment.json
          python3 tool/report_experience_checks.py --full
      - name: Publish diagnostic report on this PR
        if: always() && hashFiles('build/experience-diagnostics/comment.json') != ''
        env:
          GH_TOKEN: ${{ github.token }}
          REPOSITORY: ${{ github.repository }}
          PR_NUMBER: ${{ github.event.pull_request.number }}
        run: gh api --method POST "repos/$REPOSITORY/issues/$PR_NUMBER/comments" --input build/experience-diagnostics/comment.json
      - name: Preserve full diagnostic output
        if: always()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v4.4.3
        with:
          name: experience-diagnostics
          path: build/experience-diagnostics/
          retention-days: 7
