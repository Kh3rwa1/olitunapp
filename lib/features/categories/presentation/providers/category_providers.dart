// Repository wiring lives in the data layer (data/di/category_di.dart) so the
// Appwrite SDK stays out of presentation. Re-exported here for compatibility.

export '../../data/di/category_di.dart';
