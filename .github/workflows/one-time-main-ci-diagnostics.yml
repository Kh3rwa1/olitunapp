name: One-time main diagnostics cleanup

on:
  push:
    branches:
      - fix/main-ci-after-leaderboard-animation
    paths:
      - .github/workflows/one-time-main-ci-diagnostics.yml

permissions:
  contents: write

jobs:
  cleanup:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683
        with:
          fetch-depth: 0
      - name: Remove completed diagnostic artifacts
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git rm --ignore-unmatch \
            .github/workflows/one-time-main-ci-diagnostics.yml \
            .github/workflows/one-time-main-web-diagnostics.yml \
            ci-main-diagnostics.txt \
            ci-main-web-diagnostics.txt \
            ci-main-rerun.txt
          git commit -m 'ci: remove completed main diagnostics'
          git push
