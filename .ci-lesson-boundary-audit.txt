===== functions/_shared/delete_account_core.js =====
   434: 
   435:       while (purchIterations < maxPurchIterations) {
   436:         purchIterations++;
>  437:         const purchases = await databases.listDocuments(databaseId, 'course_purchases', [
   438:           Query.equal('userId', userId),
   439:           Query.limit(PAGE_LIMIT),
   440:         ]);

   446: 
   447:         for (const purchase of pendingAnonymization) {
   448:           try {
>  449:             await databases.updateDocument(databaseId, 'course_purchases', purchase.$id, {
   450:               userId: ANONYMIZED_USER_ID,
   451:               userEmail: 'anonymized@deleted.local',
   452:               userName: 'Anonymized User',

   461:       }
   462: 
   463:       if (purchIterations >= maxPurchIterations) {
>  464:         const remainingPurchases = await databases.listDocuments(databaseId, 'course_purchases', [
   465:           Query.equal('userId', userId),
   466:           Query.limit(1),
   467:         ]);

   508:       }
   509: 
   510:       if (cleanupSuccess) {
>  511:         const purchCheck = await databases.listDocuments(databaseId, 'course_purchases', [
   512:           Query.equal('userId', userId),
   513:           Query.limit(1),
   514:         ]);


===== functions/_shared/payment_reconcile.js =====
    21:  * Checks payment_attempts in 'reconciliation_required' or stuck 'in_progress' status.
    22:  * Queries Razorpay status via API if providerOrderId exists.
    23:  * For paid orders, verifies the captured payment amount against the attempt's
>   24:  * expected price (paise) before writing the canonical course_purchases ledger
    25:  * row (same stableId(`${userId}:${categoryId}`) key, schema, and document
    26:  * permissions as createRazorpayOrder/verifyCoursePurchase/razorpayWebhook).
    27:  * The ledger row is written BEFORE the attempt is marked verified, so a crash

   145:           try {
   146:             await databases.updateDocument(
   147:               databaseId,
>  148:               'course_purchases',
   149:               purchaseId,
   150:               verifiedLedger,
   151:               documentPermissions,

   154:             if (purchErr.code === 404) {
   155:               await databases.createDocument(
   156:                 databaseId,
>  157:                 'course_purchases',
   158:                 purchaseId,
   159:                 verifiedLedger,
   160:                 documentPermissions,

   193: }
   194: 
   195: /**
>  196:  * Reconciles disputed course_purchases against Razorpay authoritative dispute state.
   197:  *
   198:  * Scans course_purchases where status == 'disputed'.
   199:  * Queries Razorpay payment dispute endpoint.

   200:  * If latest dispute is 'won' (or closed with merchant winning):
   201:  *   Restores course_purchases status to 'verified'.

   202:  * If latest dispute is 'lost':
   203:  *   Revokes entitlement, course_purchases status to 'revoked'.
   204:  * If latest dispute is 'open' or 'under_review':

   205:  *   Preserves 'disputed' containment status.
   206:  */

   217:   const databases = withPaymentStateGuard(inputDatabases, { event: 'reconcile.dispute' });
   218: 
   219:   try {
>  220:     const disputedPurchases = await databases.listDocuments(databaseId, 'course_purchases', [
   221:       Query.equal('status', 'disputed'),
   222:       Query.limit(50),
   223:     ]);

   311:           // Entitlement recovered
   312:           await databases.updateDocument(
   313:             databaseId,
>  314:             'course_purchases',
   315:             purchase.$id,
   316:             { status: 'verified' },
   317:           );

   321:           // Entitlement revoked
   322:           await databases.updateDocument(
   323:             databaseId,
>  324:             'course_purchases',
   325:             purchase.$id,
   326:             { status: 'revoked' },
   327:           );


===== functions/_shared/payment_state.js =====
    97: }
    98: 
    99: // Request-scoped adapter: preserve each handler's gateway/signature/claim logic,
>  100: // but require every course_purchases update to pass the same transaction gate.
   101: // Reads retain order/payment binding so a late refund cannot affect a repurchase.
   102: export function withPaymentStateGuard(databases, { event = 'capture' } = {}) {
   103:   if (databases?.__isPaymentStateGuarded && databases.__guardedEvent === event) {

   118:       if (prop === 'getDocument') return async (...args) => {
   119:         const col = typeof args[0] === 'object' ? args[0].collectionId : args[1];
   120:         const doc = await target.getDocument(...args);
>  121:         if (col === 'course_purchases') {
   122:           if (capture) assertCaptureAllowed(doc);
   123:           remember(doc);
   124:         }

   127:       if (prop === 'listDocuments') return async (...args) => {
   128:         const col = typeof args[0] === 'object' ? args[0].collectionId : args[1];
   129:         const result = await target.listDocuments(...args);
>  130:         if (col === 'course_purchases') result.documents.forEach(remember);
   131:         return result;
   132:       };
   133:       if (prop === 'createDocument') return async (...args) => {

   134:         const col = typeof args[0] === 'object' ? args[0].collectionId : args[1];
   135:         const data = typeof args[0] === 'object' ? args[0].data : args[3];
>  136:         if (col === 'course_purchases' && data?.status === 'verified') {
   137:           throw new PaymentStateConflict('Missing purchase ledger requires manual reconciliation.');
   138:         }
   139:         return target.createDocument(...args);

   143:           databaseId: args[0], collectionId: args[1], documentId: args[2],
   144:           data: args[3], permissions: args[4],
   145:         };
>  146:         if (options.collectionId !== 'course_purchases') return target.updateDocument(...args);
   147:         if (typeof target.createTransaction !== 'function' || typeof target.updateTransaction !== 'function') {
   148:           throw Object.assign(new Error('Payment transactions are unavailable.'), { code: 503 });
   149:         }


===== functions/admin-maintenance/src/main.js =====
    25:   'words',
    26:   'numbers',
    27:   'letters',
>   28:   'lessons',
    29:   'categories',
    30: ];
    31: 

   295:   // 2. Fetch current purchase document
   296:   let purchase;
   297:   try {
>  298:     purchase = await databases.getDocument(databaseId, 'course_purchases', purchaseId);
   299:   } catch (err) {
   300:     if (err.code === 404 || err.status === 404) {
   301:       throw Object.assign(new Error(`Purchase ${purchaseId} not found.`), { status: 404 });

   542:     }
   543:   }
   544: 
>  545:   // 9. Phase 2: Update course_purchases via guarded databases (Skipped if ledger was already committed)
   546:   let updatedPurchase = purchase;
   547:   let ledgerAlreadyCommitted = isThisClaimLedgerCommitted;
   548: 

   559:     try {
   560:       updatedPurchase = await guardedDb.updateDocument(
   561:         databaseId,
>  562:         'course_purchases',
   563:         purchaseId,
   564:         {
   565:           status: isFullRefund ? 'refunded' : purchase.status,

   583:         // Treated as not-committed below.
   584:       }
   585:       try {
>  586:         freshPurchase = await databases.getDocument(databaseId, 'course_purchases', purchaseId);
   587:       } catch {
   588:         // Treated as not-committed below.
   589:       }

   619:           },
   620:         );
   621:       } catch {
>  622:         // Non-fatal if claim status update fails after course_purchases is committed
   623:       }
   624:     }
   625:   }


===== functions/admin-maintenance/src/shared/payment_state.js =====
    97: }
    98: 
    99: // Request-scoped adapter: preserve each handler's gateway/signature/claim logic,
>  100: // but require every course_purchases update to pass the same transaction gate.
   101: // Reads retain order/payment binding so a late refund cannot affect a repurchase.
   102: export function withPaymentStateGuard(databases, { event = 'capture' } = {}) {
   103:   if (databases?.__isPaymentStateGuarded && databases.__guardedEvent === event) {

   118:       if (prop === 'getDocument') return async (...args) => {
   119:         const col = typeof args[0] === 'object' ? args[0].collectionId : args[1];
   120:         const doc = await target.getDocument(...args);
>  121:         if (col === 'course_purchases') {
   122:           if (capture) assertCaptureAllowed(doc);
   123:           remember(doc);
   124:         }

   127:       if (prop === 'listDocuments') return async (...args) => {
   128:         const col = typeof args[0] === 'object' ? args[0].collectionId : args[1];
   129:         const result = await target.listDocuments(...args);
>  130:         if (col === 'course_purchases') result.documents.forEach(remember);
   131:         return result;
   132:       };
   133:       if (prop === 'createDocument') return async (...args) => {

   134:         const col = typeof args[0] === 'object' ? args[0].collectionId : args[1];
   135:         const data = typeof args[0] === 'object' ? args[0].data : args[3];
>  136:         if (col === 'course_purchases' && data?.status === 'verified') {
   137:           throw new PaymentStateConflict('Missing purchase ledger requires manual reconciliation.');
   138:         }
   139:         return target.createDocument(...args);

   143:           databaseId: args[0], collectionId: args[1], documentId: args[2],
   144:           data: args[3], permissions: args[4],
   145:         };
>  146:         if (options.collectionId !== 'course_purchases') return target.updateDocument(...args);
   147:         if (typeof target.createTransaction !== 'function' || typeof target.updateTransaction !== 'function') {
   148:           throw Object.assign(new Error('Payment transactions are unavailable.'), { code: 503 });
   149:         }


===== functions/admin-maintenance/test/main.test.js =====
   111:       async listDocuments(_databaseId, collectionId) {
   112:         return {
   113:           documents:
>  114:             collectionId === 'lessons'
   115:               ? [{ $id: 'lesson-1', titleLatin: 'Intro' }]
   116:               : [],
   117:         };

   207:         lessons: [
   208:           {
   209:             $id: 'lesson-1',
>  210:             $collectionId: 'lessons',
   211:             $databaseId: 'olitun_db',
   212:             titleLatin: 'Intro to Letters',
   213:             $permissions: ['read("users")', 'write("admins")'],

   265:     // Verify document deletion was called on all content collections
   266:     // CONTENT_COLLECTIONS: quizzes, sentences, words, numbers, letters, lessons, categories
   267:     assert.equal(deletedDocs.length, 7);
>  268:     assert.ok(deletedDocs.some((d) => d.collectionId === 'lessons' && d.documentId === 'lessons-old-id'));
   269:     assert.ok(deletedDocs.some((d) => d.collectionId === 'quizzes' && d.documentId === 'quizzes-old-id'));
   270: 
   271:     // Verify document creation was called with sanitized properties and original permissions

   272:     assert.equal(createdDocs.length, 1);
   273:     assert.deepEqual(createdDocs[0], {
>  274:       collectionId: 'lessons',
   275:       documentId: 'lesson-1',
   276:       data: {
   277:         titleLatin: 'Intro to Letters',


===== functions/backupCollections/src/main.js =====
     5: export const BACKUP_BUCKET_ID = process.env.ADMIN_BACKUP_BUCKET_ID || 'admin_backups';
     6: export const CONTENT_COLLECTIONS = [
     7:   'categories',
>    8:   'lessons',
     9:   'quizzes',
    10:   'letters',
    11:   'numbers',


===== functions/createRazorpayOrder/src/main.js =====
    86:     try {
    87:       // 1. Check if a verified purchase already exists
    88:       try {
>   89:         const existing = await databases.getDocument(databaseId, 'course_purchases', purchaseId);
    90:         if (existing.status === 'verified') {
    91:           return res.json({ ok: false, message: 'Category already unlocked', purchase: existing });
    92:         }


===== functions/createRazorpayOrder/src/purchase_ledger.js =====
    23:   // different devices submit different client idempotency keys concurrently.
    24:   try {
    25:     return await databases.createDocument(
>   26:       databaseId, 'course_purchases', purchaseId, data, permissions,
    27:     );
    28:   } catch (err) {
    29:     if (err.code !== 409) throw err;

    30:   }
    31: 
>   32:   const current = await databases.getDocument(databaseId, 'course_purchases', purchaseId);
    33:   if (current.userId !== data.userId || current.categoryId !== data.categoryId) {
    34:     throw new CheckoutConflict('Purchase ownership does not match the checkout.');
    35:   }

    45:   let committed = false;
    46:   try {
    47:     const latest = await databases.getDocument({
>   48:       databaseId, collectionId: 'course_purchases', documentId: purchaseId,
    49:       transactionId: transaction.$id,
    50:     });
    51:     if (latest.userId !== data.userId || latest.categoryId !== data.categoryId) {

    56:       throw new CheckoutConflict();
    57:     }
    58:     const next = await databases.updateDocument({
>   59:       databaseId, collectionId: 'course_purchases', documentId: purchaseId,
    60:       data: { ...data, refundedAmountPaise: 0, refundStatus: '' },
    61:       permissions, transactionId: transaction.$id,
    62:     });


===== functions/createRazorpayOrder/src/shared/payment_reconcile.js =====
    21:  * Checks payment_attempts in 'reconciliation_required' or stuck 'in_progress' status.
    22:  * Queries Razorpay status via API if providerOrderId exists.
    23:  * For paid orders, verifies the captured payment amount against the attempt's
>   24:  * expected price (paise) before writing the canonical course_purchases ledger
    25:  * row (same stableId(`${userId}:${categoryId}`) key, schema, and document
    26:  * permissions as createRazorpayOrder/verifyCoursePurchase/razorpayWebhook).
    27:  * The ledger row is written BEFORE the attempt is marked verified, so a crash

   145:           try {
   146:             await databases.updateDocument(
   147:               databaseId,
>  148:               'course_purchases',
   149:               purchaseId,
   150:               verifiedLedger,
   151:               documentPermissions,

   154:             if (purchErr.code === 404) {
   155:               await databases.createDocument(
   156:                 databaseId,
>  157:                 'course_purchases',
   158:                 purchaseId,
   159:                 verifiedLedger,
   160:                 documentPermissions,

   193: }
   194: 
   195: /**
>  196:  * Reconciles disputed course_purchases against Razorpay authoritative dispute state.
   197:  *
   198:  * Scans course_purchases where status == 'disputed'.
   199:  * Queries Razorpay payment dispute endpoint.

   200:  * If latest dispute is 'won' (or closed with merchant winning):
   201:  *   Restores course_purchases status to 'verified'.

   202:  * If latest dispute is 'lost':
   203:  *   Revokes entitlement, course_purchases status to 'revoked'.
   204:  * If latest dispute is 'open' or 'under_review':

   205:  *   Preserves 'disputed' containment status.
   206:  */

   217:   const databases = withPaymentStateGuard(inputDatabases, { event: 'reconcile.dispute' });
   218: 
   219:   try {
>  220:     const disputedPurchases = await databases.listDocuments(databaseId, 'course_purchases', [
   221:       Query.equal('status', 'disputed'),
   222:       Query.limit(50),
   223:     ]);

   311:           // Entitlement recovered
   312:           await databases.updateDocument(
   313:             databaseId,
>  314:             'course_purchases',
   315:             purchase.$id,
   316:             { status: 'verified' },
   317:           );

   321:           // Entitlement revoked
   322:           await databases.updateDocument(
   323:             databaseId,
>  324:             'course_purchases',
   325:             purchase.$id,
   326:             { status: 'revoked' },
   327:           );


===== functions/createRazorpayOrder/src/shared/payment_state.js =====
    97: }
    98: 
    99: // Request-scoped adapter: preserve each handler's gateway/signature/claim logic,
>  100: // but require every course_purchases update to pass the same transaction gate.
   101: // Reads retain order/payment binding so a late refund cannot affect a repurchase.
   102: export function withPaymentStateGuard(databases, { event = 'capture' } = {}) {
   103:   if (databases?.__isPaymentStateGuarded && databases.__guardedEvent === event) {

   118:       if (prop === 'getDocument') return async (...args) => {
   119:         const col = typeof args[0] === 'object' ? args[0].collectionId : args[1];
   120:         const doc = await target.getDocument(...args);
>  121:         if (col === 'course_purchases') {
   122:           if (capture) assertCaptureAllowed(doc);
   123:           remember(doc);
   124:         }

   127:       if (prop === 'listDocuments') return async (...args) => {
   128:         const col = typeof args[0] === 'object' ? args[0].collectionId : args[1];
   129:         const result = await target.listDocuments(...args);
>  130:         if (col === 'course_purchases') result.documents.forEach(remember);
   131:         return result;
   132:       };
   133:       if (prop === 'createDocument') return async (...args) => {

   134:         const col = typeof args[0] === 'object' ? args[0].collectionId : args[1];
   135:         const data = typeof args[0] === 'object' ? args[0].data : args[3];
>  136:         if (col === 'course_purchases' && data?.status === 'verified') {
   137:           throw new PaymentStateConflict('Missing purchase ledger requires manual reconciliation.');
   138:         }
   139:         return target.createDocument(...args);

   143:           databaseId: args[0], collectionId: args[1], documentId: args[2],
   144:           data: args[3], permissions: args[4],
   145:         };
>  146:         if (options.collectionId !== 'course_purchases') return target.updateDocument(...args);
   147:         if (typeof target.createTransaction !== 'function' || typeof target.updateTransaction !== 'function') {
   148:           throw Object.assign(new Error('Payment transactions are unavailable.'), { code: 503 });
   149:         }


===== functions/delete-account/src/main.js =====
   437: 
   438:       while (purchIterations < maxPurchIterations) {
   439:         purchIterations++;
>  440:         const purchases = await databases.listDocuments(databaseId, 'course_purchases', [
   441:           Query.equal('userId', userId),
   442:           Query.limit(PAGE_LIMIT),
   443:         ]);

   449: 
   450:         for (const purchase of pendingAnonymization) {
   451:           try {
>  452:             await databases.updateDocument(databaseId, 'course_purchases', purchase.$id, {
   453:               userId: ANONYMIZED_USER_ID,
   454:               userEmail: 'anonymized@deleted.local',
   455:               userName: 'Anonymized User',

   464:       }
   465: 
   466:       if (purchIterations >= maxPurchIterations) {
>  467:         const remainingPurchases = await databases.listDocuments(databaseId, 'course_purchases', [
   468:           Query.equal('userId', userId),
   469:           Query.limit(1),
   470:         ]);

   511:       }
   512: 
   513:       if (cleanupSuccess) {
>  514:         const purchCheck = await databases.listDocuments(databaseId, 'course_purchases', [
   515:           Query.equal('userId', userId),
   516:           Query.limit(1),
   517:         ]);


===== functions/getAuthorizedLesson/src/list-lessons.js =====
   105:   const purchases = await listAllByCursor({
   106:     databases,
   107:     databaseId,
>  108:     collectionId: 'course_purchases',
   109:     baseQueries: [Query.equal('userId', callerUserId)],
   110:   });
   111:   const byCategory = new Map();

   157:   if (cursor) queries.push(Query.cursorAfter(cursor));
   158: 
   159:   const [lessonResult, categories] = await Promise.all([
>  160:     databases.listDocuments(databaseId, 'lessons', queries),
   161:     loadCategoryMap({ databases, databaseId, categoryId }),
   162:   ]);
   163:   const lessonDocuments = Array.isArray(lessonResult.documents)


===== functions/getAuthorizedLesson/src/main.js =====
   362: 
   363:     let lessonDoc;
   364:     try {
>  365:       lessonDoc = await databases.getDocument(databaseId, 'lessons', lessonId);
   366:     } catch (err) {
   367:       if (err.code === 404) {
   368:         return res.json({ ok: false, error: 'lesson_not_found', message: 'Lesson not found' }, 404);

   396:     let purchases = [];
   397:     if (callerUserId) {
   398:       try {
>  399:         const purchaseResult = await databases.listDocuments(databaseId, 'course_purchases', [
   400:           Query.equal('userId', callerUserId),
   401:           Query.equal('categoryId', lessonDoc.categoryId),
   402:           Query.limit(10),


===== functions/razorpayWebhook/src/main.js =====
   113: 
   114:         let pendingPurchase;
   115:         try {
>  116:           pendingPurchase = await databases.getDocument(databaseId, 'course_purchases', purchaseId);
   117:         } catch (err) {
   118:           if (err.code === 404) {
   119:             return res.json({ ok: false, message: 'Pending purchase document not found' }, 404);

   204: 
   205:         await databases.updateDocument(
   206:           databaseId,
>  207:           'course_purchases',
   208:           purchaseId,
   209:           {
   210:             providerPaymentId: paymentId,

   239:         if (userId && categoryId) {
   240:           const purchaseId = stableId(`${userId}:${categoryId}`);
   241:           try {
>  242:             const pendingPurchase = await databases.getDocument(databaseId, 'course_purchases', purchaseId);
   243: 
   244:             if (pendingPurchase.providerOrderId === orderId &&
   245:                 pendingPurchase.status !== 'verified' &&

   246:                 pendingPurchase.status !== 'refunded') {
>  247:               await databases.updateDocument(databaseId, 'course_purchases', purchaseId, {
   248:                 status: 'failed',
   249:                 failureReason: payment.error_description || 'Payment failed'
   250:               });

   271:           return res.json({ ok: false, message: 'Missing refundId or paymentId in payload' }, 400);
   272:         }
   273: 
>  274:         const matchingDocs = await databases.listDocuments(databaseId, 'course_purchases', [
   275:           Query.equal('providerPaymentId', paymentId),
   276:           Query.limit(5)
   277:         ]);

   409:             return res.json({ ok: false, message: 'Authoritative payment refund state unavailable' }, 503);
   410:           }
   411: 
>  412:           // 4. Update course_purchases ledger using authoritative total with FENCING & MONOTONIC PROTECTIONS
   413:           let latestDoc = targetPurchaseDoc;
   414:           try {
   415:             latestDoc = await databases.getDocument(databaseId, 'course_purchases', purchaseId);

   416:           } catch (readErr) {
   417:             error(`refund fencing: could not re-read ledger ${purchaseId}, using stale snapshot: ${readErr?.message}`);
   418:           }

   429:             return res.json({ ok: false, message: 'Payment lock invalidated' }, 503);
   430:           }
   431: 
>  432:           // FENCING CHECK 2: Monotonic Epoch Fencing Token on course_purchases
   433:           const existingRefundEpoch = Number(latestDoc.refundEpoch || 0);
   434:           if (targetEpoch < existingRefundEpoch) {
   435:             error(`FENCING REJECTED: Target epoch ${targetEpoch} is older than stored ledger refundEpoch ${existingRefundEpoch}. Stale worker write prevented.`);

   443:           const finalRefundedPaise = Math.max(previousRefundedPaise, authoritativeRefundPaise);
   444:           const isFullyRefunded = finalRefundedPaise >= expectedPaise || expectedPaise === 0;
   445: 
>  446:           await databases.updateDocument(databaseId, 'course_purchases', latestDoc.$id, {
   447:             status: isFullyRefunded ? 'refunded' : 'verified',
   448:             refundStatus: isFullyRefunded ? 'fully_refunded' : 'partially_refunded',
   449:             refundedAmountPaise: finalRefundedPaise,

   488:         const disputeStatus = String(dispute.status || '').trim();
   489: 
   490:         if (paymentId) {
>  491:           const matchingDocs = await databases.listDocuments(databaseId, 'course_purchases', [
   492:             Query.equal('providerPaymentId', paymentId),
   493:             Query.limit(5)
   494:           ]);

   514:               targetStatus = 'refunded';
   515:             }
   516: 
>  517:             await databases.updateDocument(databaseId, 'course_purchases', doc.$id, {
   518:               status: targetStatus
   519:             });
   520:           }


===== functions/razorpayWebhook/src/shared/payment_state.js =====
    97: }
    98: 
    99: // Request-scoped adapter: preserve each handler's gateway/signature/claim logic,
>  100: // but require every course_purchases update to pass the same transaction gate.
   101: // Reads retain order/payment binding so a late refund cannot affect a repurchase.
   102: export function withPaymentStateGuard(databases, { event = 'capture' } = {}) {
   103:   if (databases?.__isPaymentStateGuarded && databases.__guardedEvent === event) {

   118:       if (prop === 'getDocument') return async (...args) => {
   119:         const col = typeof args[0] === 'object' ? args[0].collectionId : args[1];
   120:         const doc = await target.getDocument(...args);
>  121:         if (col === 'course_purchases') {
   122:           if (capture) assertCaptureAllowed(doc);
   123:           remember(doc);
   124:         }

   127:       if (prop === 'listDocuments') return async (...args) => {
   128:         const col = typeof args[0] === 'object' ? args[0].collectionId : args[1];
   129:         const result = await target.listDocuments(...args);
>  130:         if (col === 'course_purchases') result.documents.forEach(remember);
   131:         return result;
   132:       };
   133:       if (prop === 'createDocument') return async (...args) => {

   134:         const col = typeof args[0] === 'object' ? args[0].collectionId : args[1];
   135:         const data = typeof args[0] === 'object' ? args[0].data : args[3];
>  136:         if (col === 'course_purchases' && data?.status === 'verified') {
   137:           throw new PaymentStateConflict('Missing purchase ledger requires manual reconciliation.');
   138:         }
   139:         return target.createDocument(...args);

   143:           databaseId: args[0], collectionId: args[1], documentId: args[2],
   144:           data: args[3], permissions: args[4],
   145:         };
>  146:         if (options.collectionId !== 'course_purchases') return target.updateDocument(...args);
   147:         if (typeof target.createTransaction !== 'function' || typeof target.updateTransaction !== 'function') {
   148:           throw Object.assign(new Error('Payment transactions are unavailable.'), { code: 503 });
   149:         }


===== functions/reconcileOrphanedDeletions/src/shared/delete_account_core.js =====
   434: 
   435:       while (purchIterations < maxPurchIterations) {
   436:         purchIterations++;
>  437:         const purchases = await databases.listDocuments(databaseId, 'course_purchases', [
   438:           Query.equal('userId', userId),
   439:           Query.limit(PAGE_LIMIT),
   440:         ]);

   446: 
   447:         for (const purchase of pendingAnonymization) {
   448:           try {
>  449:             await databases.updateDocument(databaseId, 'course_purchases', purchase.$id, {
   450:               userId: ANONYMIZED_USER_ID,
   451:               userEmail: 'anonymized@deleted.local',
   452:               userName: 'Anonymized User',

   461:       }
   462: 
   463:       if (purchIterations >= maxPurchIterations) {
>  464:         const remainingPurchases = await databases.listDocuments(databaseId, 'course_purchases', [
   465:           Query.equal('userId', userId),
   466:           Query.limit(1),
   467:         ]);

   508:       }
   509: 
   510:       if (cleanupSuccess) {
>  511:         const purchCheck = await databases.listDocuments(databaseId, 'course_purchases', [
   512:           Query.equal('userId', userId),
   513:           Query.limit(1),
   514:         ]);


===== functions/reconcilePaymentAttempts/src/reconcile.js =====
    11:  * Checks payment_attempts in 'reconciliation_required' or stuck 'in_progress' status.
    12:  * Queries Razorpay status via API if providerOrderId exists.
    13:  * For paid orders, verifies the captured payment amount against the attempt's
>   14:  * expected price (paise) before writing the canonical course_purchases ledger
    15:  * row (same stableId(`${userId}:${categoryId}`) key, schema, and document
    16:  * permissions as createRazorpayOrder/verifyCoursePurchase/razorpayWebhook).
    17:  * The ledger row is written BEFORE the attempt is marked verified, so a crash

   135:           try {
   136:             await databases.updateDocument(
   137:               databaseId,
>  138:               'course_purchases',
   139:               purchaseId,
   140:               verifiedLedger,
   141:               documentPermissions,

   144:             if (purchErr.code === 404) {
   145:               await databases.createDocument(
   146:                 databaseId,
>  147:                 'course_purchases',
   148:                 purchaseId,
   149:                 verifiedLedger,
   150:                 documentPermissions,


===== functions/reconcilePaymentAttempts/src/shared/payment_reconcile.js =====
    21:  * Checks payment_attempts in 'reconciliation_required' or stuck 'in_progress' status.
    22:  * Queries Razorpay status via API if providerOrderId exists.
    23:  * For paid orders, verifies the captured payment amount against the attempt's
>   24:  * expected price (paise) before writing the canonical course_purchases ledger
    25:  * row (same stableId(`${userId}:${categoryId}`) key, schema, and document
    26:  * permissions as createRazorpayOrder/verifyCoursePurchase/razorpayWebhook).
    27:  * The ledger row is written BEFORE the attempt is marked verified, so a crash

   145:           try {
   146:             await databases.updateDocument(
   147:               databaseId,
>  148:               'course_purchases',
   149:               purchaseId,
   150:               verifiedLedger,
   151:               documentPermissions,

   154:             if (purchErr.code === 404) {
   155:               await databases.createDocument(
   156:                 databaseId,
>  157:                 'course_purchases',
   158:                 purchaseId,
   159:                 verifiedLedger,
   160:                 documentPermissions,

   193: }
   194: 
   195: /**
>  196:  * Reconciles disputed course_purchases against Razorpay authoritative dispute state.
   197:  *
   198:  * Scans course_purchases where status == 'disputed'.
   199:  * Queries Razorpay payment dispute endpoint.

   200:  * If latest dispute is 'won' (or closed with merchant winning):
   201:  *   Restores course_purchases status to 'verified'.

   202:  * If latest dispute is 'lost':
   203:  *   Revokes entitlement, course_purchases status to 'revoked'.
   204:  * If latest dispute is 'open' or 'under_review':

   205:  *   Preserves 'disputed' containment status.
   206:  */

   217:   const databases = withPaymentStateGuard(inputDatabases, { event: 'reconcile.dispute' });
   218: 
   219:   try {
>  220:     const disputedPurchases = await databases.listDocuments(databaseId, 'course_purchases', [
   221:       Query.equal('status', 'disputed'),
   222:       Query.limit(50),
   223:     ]);

   311:           // Entitlement recovered
   312:           await databases.updateDocument(
   313:             databaseId,
>  314:             'course_purchases',
   315:             purchase.$id,
   316:             { status: 'verified' },
   317:           );

   321:           // Entitlement revoked
   322:           await databases.updateDocument(
   323:             databaseId,
>  324:             'course_purchases',
   325:             purchase.$id,
   326:             { status: 'revoked' },
   327:           );


===== functions/reconcilePaymentAttempts/src/shared/payment_state.js =====
    97: }
    98: 
    99: // Request-scoped adapter: preserve each handler's gateway/signature/claim logic,
>  100: // but require every course_purchases update to pass the same transaction gate.
   101: // Reads retain order/payment binding so a late refund cannot affect a repurchase.
   102: export function withPaymentStateGuard(databases, { event = 'capture' } = {}) {
   103:   if (databases?.__isPaymentStateGuarded && databases.__guardedEvent === event) {

   118:       if (prop === 'getDocument') return async (...args) => {
   119:         const col = typeof args[0] === 'object' ? args[0].collectionId : args[1];
   120:         const doc = await target.getDocument(...args);
>  121:         if (col === 'course_purchases') {
   122:           if (capture) assertCaptureAllowed(doc);
   123:           remember(doc);
   124:         }

   127:       if (prop === 'listDocuments') return async (...args) => {
   128:         const col = typeof args[0] === 'object' ? args[0].collectionId : args[1];
   129:         const result = await target.listDocuments(...args);
>  130:         if (col === 'course_purchases') result.documents.forEach(remember);
   131:         return result;
   132:       };
   133:       if (prop === 'createDocument') return async (...args) => {

   134:         const col = typeof args[0] === 'object' ? args[0].collectionId : args[1];
   135:         const data = typeof args[0] === 'object' ? args[0].data : args[3];
>  136:         if (col === 'course_purchases' && data?.status === 'verified') {
   137:           throw new PaymentStateConflict('Missing purchase ledger requires manual reconciliation.');
   138:         }
   139:         return target.createDocument(...args);

   143:           databaseId: args[0], collectionId: args[1], documentId: args[2],
   144:           data: args[3], permissions: args[4],
   145:         };
>  146:         if (options.collectionId !== 'course_purchases') return target.updateDocument(...args);
   147:         if (typeof target.createTransaction !== 'function' || typeof target.updateTransaction !== 'function') {
   148:           throw Object.assign(new Error('Payment transactions are unavailable.'), { code: 503 });
   149:         }


===== functions/test/admin_refund_operation_identity.test.js =====
    18: };
    19: 
    20: function purchaseDoc(db) {
>   21:   return db.getDocument('olitun_db', 'course_purchases', 'purchase');
    22: }
    23: 
    24: function auditDocs(db) {

   411:     const db = new MemoryDatabase({ ...basePurchase });
   412:     // Simulate a crash between the ledger commit and any claim/audit write:
   413:     // fail every refund_claims status write AND the audit write on the first
>  414:     // attempt. The ledger commit itself (course_purchases, carrying
   415:     // lastRefundClaimId) still lands.
   416:     let faultActive = true;
   417:     const originalUpdate = db.updateDocument.bind(db);


===== functions/test/admin_restore_runtime.test.js =====
     2: import test from 'node:test';
     3: import { restoreValidatedContent } from '../admin-maintenance/src/restore_backup.js';
     4: 
>    5: const collectionIds = ['quizzes', 'sentences', 'words', 'numbers', 'letters', 'lessons', 'categories'];
     6: function backup() {
     7:   return {
     8:     schemaVersion: 1,


===== functions/test/authorized_lesson.test.js =====
    37: } = {}) {
    38:   return {
    39:     async getDocument(databaseId, collectionId, documentId) {
>   40:       if (collectionId === 'lessons') {
    41:         const lesson = lessons[documentId];
    42:         if (!lesson) {
    43:           const err = new Error('Lesson document not found');

    58:       throw new Error(`Unexpected collection: ${collectionId}`);
    59:     },
    60:     async listDocuments(databaseId, collectionId, queries) {
>   61:       if (collectionId === 'course_purchases') {
    62:         let filtered = [...purchases];
    63:         for (const query of queries) {
    64:           let parsed;


===== functions/test/authorized_lesson_listing.test.js =====
    49:     async listDocuments(databaseId, collectionId, queries) {
    50:       calls.push({ operation: 'listDocuments', collectionId, queries });
    51:       const parsed = parseQueries(queries);
>   52:       let documents = collectionId === 'lessons'
    53:         ? [...lessons]
    54:         : collectionId === 'categories'
    55:           ? [...categories]

    61:           documents = documents.filter((document) => values.includes(document[query.attribute]));
    62:         }
    63:       }
>   64:       if (collectionId === 'lessons') {
    65:         documents.sort((left, right) => Number(left.order || 0) - Number(right.order || 0));
    66:       }
    67:       const cursorQuery = parsed.find((query) => query.method === 'cursorAfter');

   156:   assert.deepEqual(res.body.lessons.map((item) => item.isLocked), [false, false, true]);
   157:   assert.equal(
   158:     databases.calls.filter((call) =>
>  159:       call.operation === 'listDocuments' && call.collectionId === 'course_purchases').length,
   160:     1,
   161:   );
   162: });


===== functions/test/authorized_media_access.test.js =====
    13:   const databases = {
    14:     async getDocument(db, collection, id) {
    15:       if (id === 'missing') throw Object.assign(new Error('missing'), { code: 404 });
>   16:       return collection === 'lessons' ? {
    17:         $id: 'lesson', categoryId: 'cat', order: 5,
    18:         blocks: JSON.stringify([{ type: 'audio', audioUrl: 'appwrite-storage://paid_media/file' }]),
    19:         ...lesson,


===== functions/test/checkout_ledger_regression.test.js =====
    32:     return clone(row);
    33:   }
    34:   async createDocument(db, collection, id, data, permissions = []) {
>   35:     if (collection === 'course_purchases' && this.failNextLedgerCreate) {
    36:       this.failNextLedgerCreate = false;
    37:       throw failure(503);
    38:     }

   120:   assert.equal(first.statusCode, 200);
   121:   assert.equal(second.statusCode, 200);
   122:   assert.equal(first.body.orderId, second.body.orderId);
>  123:   const current = await db.getDocument('', 'course_purchases', purchaseId);
   124:   assert.equal(current.providerOrderId, first.body.orderId);
   125:   assert.equal((await db.getDocument('', 'payment_attempts', attemptId('device-two'))).status, 'superseded');
   126: });

   132:   const results = await Promise.all(['parallel-one', 'parallel-two'].map(key => checkout(handler, key)));
   133:   assert.ok(results.every(res => res.statusCode === 200 && res.body.ok));
   134:   assert.equal(new Set(results.map(res => res.body.orderId)).size, 1);
>  135:   assert.equal((await db.getDocument('', 'course_purchases', purchaseId)).providerOrderId, results[0].body.orderId);
   136: });
   137: 
   138: test('a verified entitlement arriving during gateway creation is never downgraded', async () => {

   139:   const db = new Database();
   140:   const api = gateway();
   141:   const handler = createOrderHandler({ databases: db, fetchImpl: async (...args) => {
>  142:     db.seed('course_purchases', purchaseId, ledger({ status: 'verified', providerPaymentId: 'pay_captured', paidAmount: 499 }));
   143:     return api.fetch(...args);
   144:   } });
   145:   const result = await checkout(handler, 'verification-race');

   146:   assert.equal(result.body.ok, false);
   147:   assert.match(result.body.message, /already unlocked/);
>  148:   const current = await db.getDocument('', 'course_purchases', purchaseId);
   149:   assert.equal(current.status, 'verified');
   150:   assert.equal(current.providerPaymentId, 'pay_captured');
   151: });

   173:   assert.equal(retry.statusCode, 200);
   174:   assert.equal(retry.body.ok, true);
   175:   assert.equal(api.calls, 1);
>  176:   assert.equal((await db.getDocument('', 'course_purchases', purchaseId)).providerOrderId, retry.body.orderId);
   177: });
   178: 
   179: test('repeated storage failure never returns an unbound checkout order', async () => {

   195:   const api = gateway();
   196:   const handler = createOrderHandler({ databases: db, fetchImpl: api.fetch });
   197:   const first = await checkout(handler, 'refund-retry');
>  198:   db.seed('course_purchases', purchaseId, ledger({ status: 'refunded', providerOrderId: first.body.orderId, refundedAmountPaise: 49900 }));
   199:   const result = await checkout(handler, 'refund-retry');
   200:   assert.equal(result.statusCode, 409);
   201:   assert.equal(result.body.ok, false);

>  202:   assert.equal((await db.getDocument('', 'course_purchases', purchaseId)).status, 'refunded');
   203:   assert.notEqual((await db.getDocument('', 'payment_attempts', attemptId('refund-retry'))).status, 'reconciliation_required');
   204: });
   205: 

   210:   }) });
   211:   const result = await checkout(handler, 'malformed-response');
   212:   assert.equal(result.statusCode, 502);
>  213:   await assert.rejects(db.getDocument('', 'course_purchases', purchaseId), { code: 404 });
   214: });
   215: 
   216: test('fractional catalog rupees are rejected instead of violating the integer ledger schema', async () => {

   236: 
   237: test('refunded repurchase commits atomically and clears the prior refund total', async () => {
   238:   const db = new Database();
>  239:   db.seed('course_purchases', purchaseId, ledger({ status: 'refunded', refundStatus: 'fully_refunded', refundedAmountPaise: 49900 }));
   240:   await publish(db);
   241:   const result = await db.getDocument('', 'course_purchases', purchaseId);
   242:   assert.equal(result.providerOrderId, 'order_new');

   243:   assert.equal(result.status, 'created');
   244:   assert.equal(result.refundedAmountPaise, 0);

   248: 
   249: test('a transaction conflict rolls back without replacing a refunded purchase', async () => {
   250:   const db = new Database();
>  251:   db.seed('course_purchases', purchaseId, ledger({ status: 'refunded' }));
   252:   db.conflictOnCommit = true;
   253:   await assert.rejects(publish(db), { code: 409 });
   254:   assert.equal((await db.getDocument('', 'course_purchases', purchaseId)).providerOrderId, 'order_old');

   255:   assert.equal(db.transactions.size, 0);
   256: });
   257: 

   258: test('transaction service failure fails closed with no plain-update fallback', async () => {
   259:   const db = new Database();
>  260:   db.seed('course_purchases', purchaseId, ledger({ status: 'refunded' }));
   261:   db.commitFailure = true;
   262:   await assert.rejects(publish(db), { code: 503 });
   263:   assert.equal((await db.getDocument('', 'course_purchases', purchaseId)).status, 'refunded');

   264:   assert.equal(db.transactions.size, 0);
   265: });
   266: 

   267: test('purchase ownership mismatch is a conflict, never a replacement', async () => {
   268:   const db = new Database();
>  269:   db.seed('course_purchases', purchaseId, ledger({ userId: 'another-learner' }));
   270:   await assert.rejects(publish(db), { code: 409 });
   271:   assert.equal((await db.getDocument('', 'course_purchases', purchaseId)).userId, 'another-learner');
   272: });

   273: 
   274: test('disputed purchases cannot be silently replaced by checkout', async () => {

   275:   const db = new Database();
>  276:   db.seed('course_purchases', purchaseId, ledger({ status: 'disputed' }));
   277:   await assert.rejects(publish(db), { code: 409 });
   278:   assert.equal((await db.getDocument('', 'course_purchases', purchaseId)).status, 'disputed');
   279: });



===== functions/test/create_razorpay_order.test.js =====
   265:     const categoryId = 'cat_bought';
   266:     const purchaseId = stableId(`${userId}:${categoryId}`);
   267: 
>  268:     db.collections.set('course_purchases', new Map([
   269:       [purchaseId, { status: 'verified' }]
   270:     ]));
   271: 

   365: 
   366:     // The reconciled grant must land on the canonical stableId ledger row
   367:     // with the verified schema, not an orphaned `purch_user_category` doc.
>  368:     const ledgerRow = db.collections.get('course_purchases').get(stableId(`${userId}:${categoryId}`));
   369:     assert.ok(ledgerRow, 'canonical course_purchases ledger row was written');
   370:     assert.equal(ledgerRow.status, 'verified');
   371:     assert.equal(ledgerRow.providerPaymentId, 'pay_rec_1');

   372:     assert.equal(ledgerRow.expectedAmount, 299);

   373:     assert.equal(ledgerRow.paidAmount, 299);
>  374:     assert.ok(![...db.collections.get('course_purchases').keys()].some(k => k.startsWith('purch_')),
   375:       'no orphaned purch_user_category docs created');
   376:   });
   377: });


===== functions/test/delete_account.test.js =====
   253:     ]));
   254:     storage.files.set('b_avatars:f_pic1', 'file_data');
   255: 
>  256:     db.collections.set('course_purchases', new Map([
   257:       ['purch_1', { $id: 'purch_1', userId, amount: 499, userEmail: 'user@test.com' }]
   258:     ]));
   259: 

   283:     assert.equal(db.collections.get('user_assets').has('asset_1'), false);
   284: 
   285:     // Assert purchase anonymized
>  286:     const purch = db.collections.get('course_purchases').get('purch_1');
   287:     assert.equal(purch.userId, 'anonymized_deleted_user');
   288:     assert.equal(purch.userEmail, 'anonymized@deleted.local');
   289: 


===== functions/test/dispute_ordering_regression.test.js =====
     9:   providerPaymentId: 'payment-1', status: 'verified', expectedAmount: 500,
    10:   currency: 'INR', refundedAmountPaise: 0,
    11: };
>   12: const read = db => db.getDocument('olitun_db', 'course_purchases', 'purchase');
    13: async function apply(db, event, status) {
    14:   const guarded = withPaymentStateGuard(db, { event });
    15:   await guarded.listDocuments('olitun_db', 'course_purchases', []);

    16:   return guarded.updateDocument('olitun_db', 'course_purchases', 'purchase', { status });
    17: }
    18: 

    19: test('late won notification cannot restore a disputed entitlement', async () => {

    65:   const db = new MemoryDatabase(purchase);
    66:   const transactional = withTransactions(db);
    67:   const guard = withPaymentStateGuard(transactional, { event: 'payment.dispute.created' });
>   68:   await guard.listDocuments('olitun_db', 'course_purchases', []);
    69:   await db.updateDocument('olitun_db', 'course_purchases', 'purchase', {
    70:     providerOrderId: 'order-2', providerPaymentId: 'payment-2',
    71:   });

    72:   await assert.rejects(guard.updateDocument('olitun_db', 'course_purchases', 'purchase', { status: 'disputed' }), { code: 409 });

    73:   assert.equal((await read(db)).status, 'verified');
    74:   assert.equal((await read(db)).providerPaymentId, 'payment-2');
    75: });


===== functions/test/helpers/transaction_db.js =====
    66: 
    67: export class MemoryDatabase {
    68:   constructor(purchase) {
>   69:     this.docs = new Map(purchase ? [['course_purchases/purchase', { $id: 'purchase', ...purchase }]] : []);
    70:     this.writes = [];
    71:   }
    72:   async getDocument(_, collection, id) {


===== functions/test/payment_dispute_recovery.test.js =====
    69:     assert.equal(stats.won, 0);
    70:     assert.equal(stats.lost, 0);
    71: 
>   72:     const doc = await db.getDocument('olitun_db', 'course_purchases', 'purchase');
    73:     assert.equal(doc.status, 'disputed');
    74:   });
    75: 

   107:     assert.equal(stats.won, 1);
   108:     assert.equal(stats.lost, 0);
   109: 
>  110:     const doc = await db.getDocument('olitun_db', 'course_purchases', 'purchase');
   111:     assert.equal(doc.status, 'verified');
   112:   });
   113: 

   145:     assert.equal(stats.lost, 1);
   146:     assert.equal(stats.won, 0);
   147: 
>  148:     const doc = await db.getDocument('olitun_db', 'course_purchases', 'purchase');
   149:     assert.equal(doc.status, 'revoked');
   150:   });
   151: 

   153:     const db = new MemoryDatabase({ ...basePurchase, status: 'revoked' });
   154:     const guardedDb = withPaymentStateGuard(withTransactions(db), { event: 'payment.dispute.won' });
   155: 
>  156:     await guardedDb.listDocuments('olitun_db', 'course_purchases', []);
   157:     const result = await guardedDb.updateDocument('olitun_db', 'course_purchases', 'purchase', {
   158:       status: 'verified',
   159:     });

   160: 

   161:     // Guard returns current document without mutating
   162:     assert.equal(result.status, 'revoked');
>  163:     const doc = await db.getDocument('olitun_db', 'course_purchases', 'purchase');
   164:     assert.equal(doc.status, 'revoked');
   165:   });
   166: 

   188:     });
   189: 
   190:     // Attempt to reinstate is contained, purchase remains refunded
>  191:     const doc = await db.getDocument('olitun_db', 'course_purchases', 'purchase');
   192:     assert.equal(doc.status, 'refunded');
   193:   });
   194: 

   226:     assert.equal(stats.won, 0);
   227:     assert.equal(stats.lost, 0);
   228: 
>  229:     const doc = await db.getDocument('olitun_db', 'course_purchases', 'purchase');
   230:     assert.equal(doc.status, 'disputed');
   231:   });
   232: 

   265:     assert.equal(stats.won, 0);
   266:     assert.equal(stats.lost, 0);
   267: 
>  268:     const doc = await db.getDocument('olitun_db', 'course_purchases', 'purchase');
   269:     assert.equal(doc.status, 'disputed');
   270:   });
   271: 

   303:     assert.equal(stats.won, 1);
   304:     assert.equal(stats.lost, 0);
   305: 
>  306:     const doc = await db.getDocument('olitun_db', 'course_purchases', 'purchase');
   307:     assert.equal(doc.status, 'verified');
   308:   });
   309: 

   341:     assert.equal(stats.lost, 1);
   342:     assert.equal(stats.won, 0);
   343: 
>  344:     const doc = await db.getDocument('olitun_db', 'course_purchases', 'purchase');
   345:     assert.equal(doc.status, 'revoked');
   346:   });
   347: 

   377:     assert.equal(stats.lost, 1);
   378:     assert.equal(stats.won, 0);
   379: 
>  380:     const doc = await db.getDocument('olitun_db', 'course_purchases', 'purchase');
   381:     assert.equal(doc.status, 'revoked');
   382:   });
   383: 

   402:     assert.equal(stats.failed, 1);
   403:     assert.equal(stats.won, 0);
   404: 
>  405:     const doc = await db.getDocument('olitun_db', 'course_purchases', 'purchase');
   406:     assert.equal(doc.status, 'disputed');
   407:   });
   408: });

   496:     assert.equal(result.refundedAmountPaise, 49900);
   497:     assert.equal(result.refundEpoch, 1);
   498: 
>  499:     const purchaseDoc = await db.getDocument('olitun_db', 'course_purchases', 'purchase');
   500:     assert.equal(purchaseDoc.status, 'refunded');
   501:     assert.equal(purchaseDoc.refundStatus, 'fully_refunded');
   502:     assert.equal(purchaseDoc.refundedAmountPaise, 49900);

   539:     assert.equal(secondResult.refundStatus, 'fully_refunded');
   540: 
   541:     // Exactly one refund write occurred, no duplicate modification
>  542:     const purchaseDoc = await db.getDocument('olitun_db', 'course_purchases', 'purchase');
   543:     assert.equal(purchaseDoc.refundEpoch, 1);
   544:   });
   545: 

   595:     assert.equal(result.refundedAmountPaise, 15000);
   596:     assert.equal(result.refundEpoch, 1);
   597: 
>  598:     const purchaseDoc = await db.getDocument('olitun_db', 'course_purchases', 'purchase');
   599:     assert.equal(purchaseDoc.status, 'verified');
   600:     assert.equal(purchaseDoc.refundStatus, 'partially_refunded');
   601:     assert.equal(purchaseDoc.refundedAmountPaise, 15000);

   637:     assert.equal(secondResult.refundStatus, 'partially_refunded');
   638:     assert.equal(secondResult.refundedAmountPaise, 15000);
   639: 
>  640:     // Zero extra writes to course_purchases occurred
   641:     const writesAfterSecond = db.writes.length;
   642:     assert.equal(writesAfterSecond, writesAfterFirst);
   643:   });

   725:     );
   726: 
   727:     // Purchase ledger was updated to refunded
>  728:     const purchaseAfterFail = await db.getDocument('olitun_db', 'course_purchases', 'purchase');
   729:     assert.equal(purchaseAfterFail.status, 'refunded');
   730:     assert.equal(purchaseAfterFail.refundEpoch, 1);
   731: 

   747:     assert.equal(retryResult.status, 'refunded');
   748: 
   749:     // Purchase epoch was NOT incremented again (no duplicate purchase write)
>  750:     const purchaseAfterRetry = await db.getDocument('olitun_db', 'course_purchases', 'purchase');
   751:     assert.equal(purchaseAfterRetry.refundEpoch, 1);
   752: 
   753:     // Audit log was durably created on retry!

   763:     const originalUpdate = db.updateDocument.bind(db);
   764:     db.updateDocument = async (...args) => {
   765:       const col = typeof args[0] === 'object' ? args[0].collectionId : args[1];
>  766:       if (col === 'course_purchases' && failPurchaseUpdate) {
   767:         throw new Error('Database transaction timeout on purchase write');
   768:       }
   769:       return originalUpdate(...args);

   782:     );
   783: 
   784:     // Purchase was NOT updated yet
>  785:     const purchaseAfterFail = await db.getDocument('olitun_db', 'course_purchases', 'purchase');
   786:     assert.equal(purchaseAfterFail.status, 'verified');
   787:     assert.equal(purchaseAfterFail.refundEpoch, 0);
   788: 

   803:     assert.equal(retryResult.alreadyRefunded, false);
   804:     assert.equal(retryResult.status, 'refunded');
   805: 
>  806:     const purchaseAfterRetry = await db.getDocument('olitun_db', 'course_purchases', 'purchase');
   807:     assert.equal(purchaseAfterRetry.status, 'refunded');
   808:     assert.equal(purchaseAfterRetry.refundEpoch, 1);
   809: 

   814: 
   815:   test('Payload mismatch: reusing externalRefundId with different purchaseId or amount throws 409 Conflict', async () => {
   816:     const db = new MemoryDatabase({ ...basePurchase });
>  817:     await db.createDocument('olitun_db', 'course_purchases', 'other_purchase', {
   818:       ...basePurchase,
   819:       $id: 'other_purchase',
   820:     });

   869:     });
   870: 
   871:     // 2. Purchase ledger was committed to refunded with lastRefundClaimId: claimId
>  872:     await db.updateDocument('olitun_db', 'course_purchases', 'purchase', {
   873:       status: 'refunded',
   874:       refundStatus: 'fully_refunded',
   875:       refundedAmountPaise: 49900,


===== functions/test/payment_functions.test.js =====
    34: 
    35: const paymentClaimsSchema = JSON.parse(readFileSync(join(process.cwd(), 'test/fixtures/schema/payment_claims.json'), 'utf8'));
    36: const refundClaimsSchema = JSON.parse(readFileSync(join(process.cwd(), 'test/fixtures/schema/refund_claims.json'), 'utf8'));
>   37: const coursePurchasesSchema = JSON.parse(readFileSync(join(process.cwd(), 'test/fixtures/schema/course_purchases.json'), 'utf8'));
    38: 
    39: class SchemaAwareInMemDb {
    40:   constructor() {

    42:     this.schemas = new Map([
    43:       ['payment_claims', paymentClaimsSchema],
    44:       ['refund_claims', refundClaimsSchema],
>   45:       ['course_purchases', coursePurchasesSchema]
    46:     ]);
    47:   }
    48: 

   230:     process.env.RAZORPAY_WEBHOOK_SECRET = webhookSecret;
   231:   });
   232: 
>  233:   test('1. Schema-aware DB validates course_purchases required fields, types, and unique indexes', () => {
   234:     const db = new SchemaAwareInMemDb();
   235: 
   236:     assert.doesNotThrow(() => {

>  237:       db.setDocument('course_purchases', 'p1', {
   238:         userId: 'u1',
   239:         categoryId: 'c1',
   240:         provider: 'razorpay',

   248:     });
   249: 
   250:     assert.throws(() => {
>  251:       db.setDocument('course_purchases', 'p2', {
   252:         userId: 'u2',
   253:         status: 'verified'
   254:       });

   255:     }, /required attribute/);
   256: 
   257:     assert.throws(() => {
>  258:       db.setDocument('course_purchases', 'p3', {
   259:         userId: 'u1',
   260:         categoryId: 'c1',
   261:         provider: 'razorpay',

   283:       unlockMode: 'paid_only'
   284:     });
   285: 
>  286:     db.setDocument('course_purchases', purchaseId, {
   287:       userId,
   288:       categoryId,
   289:       provider: 'razorpay',

   351:     const paymentId = 'pay_RZP_bob';
   352:     const purchaseId = stableId(`${userId}:${categoryId}`);
   353: 
>  354:     db.setDocument('course_purchases', purchaseId, {
   355:       userId,
   356:       categoryId,
   357:       provider: 'razorpay',

   404:     const purchaseId = stableId(`${userId}:${categoryId}`);
   405:     const claimId = stableId(`claim:${paymentId}`);
   406: 
>  407:     db.setDocument('course_purchases', purchaseId, {
   408:       userId, categoryId, provider: 'razorpay', providerOrderId: orderId, providerPaymentId: paymentId,
   409:       expectedAmount: 500, paidAmount: 500, currency: 'INR', status: 'verified', createdAt: new Date().toISOString()
   410:     });

   441:     const paymentId = 'pay_lock_test';
   442:     const purchaseId = stableId('user_lock:cat_lock');
   443: 
>  444:     db.setDocument('course_purchases', purchaseId, {
   445:       userId: 'user_lock', categoryId: 'cat_lock', provider: 'razorpay', providerOrderId: 'order_lock',
   446:       providerPaymentId: paymentId, expectedAmount: 500, paidAmount: 500, refundedAmountPaise: 0,
   447:       currency: 'INR', status: 'verified', createdAt: new Date().toISOString()

   481:     const purchaseId = stableId('u_fence:c_fence');
   482: 
   483:     // Purchase ledger starts at refundEpoch = 0
>  484:     db.setDocument('course_purchases', purchaseId, {
   485:       userId: 'u_fence', categoryId: 'c_fence', provider: 'razorpay', providerOrderId: 'o_fence',
   486:       providerPaymentId: paymentId, expectedAmount: 500, paidAmount: 500, refundedAmountPaise: 0,
   487:       refundEpoch: 0, currency: 'INR', status: 'verified', createdAt: new Date().toISOString()

   514:     });
   515: 
   516:     // Verify Worker 2 updated purchase ledger to 50000 paise and set refundEpoch = 2
>  517:     let purchase = await db.getDocument('olitun_db', 'course_purchases', purchaseId);
   518:     assert.equal(purchase.status, 'refunded');
   519:     assert.equal(purchase.refundedAmountPaise, 50000);
   520:     assert.equal(purchase.refundEpoch, 2);

   550: 
   551:     // Now test a stalled Worker 1 resuming execution past lock acquisition with targetEpoch = 1
   552:     const dbStalledWorker = new SchemaAwareInMemDb();
>  553:     dbStalledWorker.setDocument('course_purchases', purchaseId, {
   554:       userId: 'u_fence', categoryId: 'c_fence', provider: 'razorpay', providerOrderId: 'o_fence',
   555:       providerPaymentId: paymentId, expectedAmount: 500, paidAmount: 500, refundedAmountPaise: 50000,
   556:       refundEpoch: 2, currency: 'INR', status: 'refunded', createdAt: new Date().toISOString()

   605:     assert.match(resStalled.body.message, /Payment ledger update in progress|Stale epoch update prevented|Payment lock invalidated/i);
   606: 
   607:     // Purchase ledger remains protected at 50000 paise & refundEpoch 2
>  608:     purchase = await dbStalledWorker.getDocument('olitun_db', 'course_purchases', purchaseId);
   609:     assert.equal(purchase.status, 'refunded');
   610:     assert.equal(purchase.refundedAmountPaise, 50000);
   611:     assert.equal(purchase.refundEpoch, 2);

   616:     const paymentId = 'pay_discriminator_test';
   617:     const purchaseId = stableId('u_disc:c_disc');
   618: 
>  619:     db.setDocument('course_purchases', purchaseId, {
   620:       userId: 'u_disc', categoryId: 'c_disc', provider: 'razorpay', providerOrderId: 'o_disc',
   621:       providerPaymentId: paymentId, expectedAmount: 500, paidAmount: 500, refundedAmountPaise: 0,
   622:       currency: 'INR', status: 'verified', createdAt: new Date().toISOString()

   666:     const categoryId = 'cat_fail_test';
   667:     const purchaseId = stableId(`${userId}:${categoryId}`);
   668: 
>  669:     db.setDocument('course_purchases', purchaseId, {
   670:       userId, categoryId, provider: 'razorpay', providerOrderId: 'order_correct_123',
   671:       expectedAmount: 500, paidAmount: 0, currency: 'INR', status: 'created', createdAt: new Date().toISOString()
   672:     });

   688:       error: createMockErrorLogger()
   689:     });
   690: 
>  691:     let purchase = await db.getDocument('olitun_db', 'course_purchases', purchaseId);
   692:     assert.equal(purchase.status, 'created');
   693: 
   694:     const matchPayload = {

   706:       error: createMockErrorLogger()
   707:     });
   708: 
>  709:     purchase = await db.getDocument('olitun_db', 'course_purchases', purchaseId);
   710:     assert.equal(purchase.status, 'failed');
   711:     assert.equal(purchase.failureReason, 'Card declined');
   712:   });

   717:     const refundId = 'ref_interrupted_11';
   718:     const purchaseId = stableId('u_rec:c_rec');
   719: 
>  720:     db.setDocument('course_purchases', purchaseId, {
   721:       userId: 'u_rec', categoryId: 'c_rec', provider: 'razorpay', providerOrderId: 'o_rec',
   722:       providerPaymentId: paymentId, expectedAmount: 500, paidAmount: 500, refundedAmountPaise: 0,
   723:       currency: 'INR', status: 'verified', createdAt: new Date().toISOString()

   751:     const claimDoc = await db.getDocument('olitun_db', 'refund_claims', claimId);
   752:     assert.equal(claimDoc.status, 'committed');
   753: 
>  754:     const purchase = await db.getDocument('olitun_db', 'course_purchases', purchaseId);
   755:     assert.equal(purchase.status, 'refunded');
   756:   });
   757: 

   760:     const paymentId = 'pay_ooo_1';
   761:     const purchaseId = stableId('u_ooo:c_ooo');
   762: 
>  763:     db.setDocument('course_purchases', purchaseId, {
   764:       userId: 'u_ooo', categoryId: 'c_ooo', provider: 'razorpay', providerOrderId: 'o_ooo',
   765:       providerPaymentId: paymentId, expectedAmount: 500, paidAmount: 500, refundedAmountPaise: 50000,
   766:       currency: 'INR', status: 'refunded', refundStatus: 'fully_refunded', createdAt: new Date().toISOString()

   785: 
   786:     assert.equal(res.statusCode, 200);
   787: 
>  788:     const purchase = await db.getDocument('olitun_db', 'course_purchases', purchaseId);
   789:     assert.equal(purchase.refundedAmountPaise, 50000);
   790:     assert.equal(purchase.status, 'refunded');
   791:   });

   794:     const db = new SchemaAwareInMemDb();
   795:     const paymentId = 'pay_duplicate_111';
   796: 
>  797:     db.setDocument('course_purchases', stableId('u1:c1'), {
   798:       userId: 'u1', categoryId: 'c1', provider: 'razorpay', providerOrderId: 'o1', providerPaymentId: paymentId,
   799:       expectedAmount: 100, paidAmount: 100, currency: 'INR', status: 'verified', createdAt: new Date().toISOString()
   800:     });

   801: 
>  802:     db.setDocument('course_purchases', stableId('u2:c2'), {
   803:       userId: 'u2', categoryId: 'c2', provider: 'razorpay', providerOrderId: 'o2', providerPaymentId: paymentId,
   804:       expectedAmount: 100, paidAmount: 100, currency: 'INR', status: 'verified', createdAt: new Date().toISOString()
   805:     });

   831:     const paymentId = 'pay_disputed_refunded_100';
   832:     const purchaseId = stableId('user_dispute:cat_dispute');
   833: 
>  834:     db.setDocument('course_purchases', purchaseId, {
   835:       userId: 'user_dispute', categoryId: 'cat_dispute', provider: 'razorpay', providerOrderId: 'order_disp',
   836:       providerPaymentId: paymentId, expectedAmount: 500, paidAmount: 500, refundedAmountPaise: 50000,
   837:       currency: 'INR', status: 'refunded', refundStatus: 'fully_refunded', createdAt: new Date().toISOString()

   853: 
   854:     assert.equal(res.statusCode, 200);
   855: 
>  856:     const purchase = await db.getDocument('olitun_db', 'course_purchases', purchaseId);
   857:     assert.equal(purchase.status, 'refunded');
   858:     assert.equal(purchase.refundStatus, 'fully_refunded');
   859:   });

   899:     const paymentId = 'pay_no_auth_total';
   900:     const purchaseId = stableId('u_fail:c_fail');
   901: 
>  902:     db.setDocument('course_purchases', purchaseId, {
   903:       userId: 'u_fail', categoryId: 'c_fail', provider: 'razorpay', providerOrderId: 'o_fail',
   904:       providerPaymentId: paymentId, expectedAmount: 500, paidAmount: 500, refundedAmountPaise: 0,
   905:       currency: 'INR', status: 'verified', createdAt: new Date().toISOString()


===== functions/test/payment_runtime_guard.test.js =====
    21:     APPWRITE_DATABASE_ID: 'olitun_db', RAZORPAY_KEY_ID: 'test-only',
    22:     RAZORPAY_KEY_SECRET: 'test-only', RAZORPAY_WEBHOOK_SECRET: 'test-only' });
    23:   const raw = new MemoryDatabase();
>   24:   raw.docs.set(`course_purchases/${purchaseId}`, ledger(extra));
    25:   const tx = withTransactions(raw);
    26:   // Mock SDK I/O, NOT the production handler factory or state gate. Default
    27:   // Appwrite entrypoints must construct and use the real guarded SDK adapter.

    30:     t.mock.method(Databases.prototype, name, (...args) => tx[name](...args));
    31:   }
    32:   t.mock.method(globalThis, 'fetch', async () => ({ ok: true, json: async () => payment }));
>   33:   return { raw, tx, current: () => raw.getDocument('', 'course_purchases', purchaseId) };
    34: }
    35: async function run(handler, req) {
    36:   const res = { status: 200, body: null, json(body, status = 200) {


===== functions/test/payment_state.test.js =====
     8:   currency: 'INR', status: 'created', ...extra });
     9: const grant = { userId: 'learner', categoryId: 'course', providerOrderId: 'order_one',
    10:   providerPaymentId: 'pay_one', expectedAmount: 499, currency: 'INR', status: 'verified' };
>   11: const update = (db, data = grant) => db.updateDocument('db', 'course_purchases', 'purchase', data);
    12: const read = db => db.getDocument('db', 'course_purchases', 'purchase');
    13: function setup(extra = {}, event = 'capture') {
    14:   const raw = new MemoryDatabase(pending(extra));

    15:   const tx = withTransactions(raw);

    23:     await assert.rejects(read(db), { code: 409 });
    24:     await assert.rejects(update(db), { code: 409 });
    25:     assert.equal((await read(raw)).status, status);
>   26:     assert.equal(raw.writes.filter(w => w.collection === 'course_purchases').length, 0);
    27:     assert.equal(tx.transactions.size, 0);
    28:   });
    29: }

    52:     await read(db);
    53:     raw.beforeCommit = async () => {
    54:       raw.beforeCommit = null;
>   55:       await raw.updateDocument('db', 'course_purchases', 'purchase', { status: terminal });
    56:     };
    57:     await assert.rejects(update(db), { code: 409 });
    58:     assert.equal((await read(raw)).status, terminal);

    62: test('gateway-latency race rechecks the latest ledger before staging the write', async () => {
    63:   const { raw, db } = setup();
    64:   await read(db);
>   65:   await raw.updateDocument('db', 'course_purchases', 'purchase', { status: 'disputed' });
    66:   await assert.rejects(update(db), { code: 409 });
    67:   assert.equal((await read(raw)).status, 'disputed');
    68: });

    83:   const raw = new MemoryDatabase();
    84:   const db = withPaymentStateGuard(withTransactions(raw));
    85:   await assert.rejects(update(db), { code: 404 });
>   86:   await assert.rejects(db.createDocument('db', 'course_purchases', 'purchase', grant), { code: 409 });
    87:   assert.equal(raw.writes.length, 0);
    88: });
    89: for (const field of ['userId', 'categoryId', 'providerOrderId']) {

   122:   const { raw, tx, db } = setup({ status: 'disputed', providerPaymentId: 'pay_one' }, 'payment.dispute.won');
   123:   await update(db, { status: 'verified' });
   124:   assert.equal((await read(raw)).status, 'disputed');
>  125:   assert.equal(raw.writes.filter(w => w.collection === 'course_purchases').length, 0);
   126:   assert.equal(tx.transactions.size, 0);
   127: });
   128: test('late refund cannot mutate a new order replacing the observed purchase', async () => {

   129:   const { raw, db } = setup({ status: 'verified', providerPaymentId: 'pay_one' }, 'refund.processed');
>  130:   await db.listDocuments('db', 'course_purchases');
   131:   await raw.updateDocument('db', 'course_purchases', 'purchase', { providerOrderId: 'order_two', providerPaymentId: '', status: 'created' });
   132:   await assert.rejects(update(db, { status: 'refunded', refundedAmountPaise: 49900, refundEpoch: 1 }), { code: 409 });
   133:   assert.equal((await read(raw)).providerOrderId, 'order_two');

   134:   assert.equal((await read(raw)).status, 'created');


===== functions/test/reconcile_paid_grant.test.js =====
   111:     assert.equal(stats.reconciled, 1);
   112:     assert.equal(stats.failed, 0);
   113: 
>  114:     const purchase = db.docs.get(`course_purchases/${EXPECTED_PURCHASE_ID}`);
   115:     assert.ok(purchase, `ledger row exists at canonical id ${EXPECTED_PURCHASE_ID}`);
   116:     assert.equal(purchase.status, 'verified');
   117:     assert.equal(purchase.provider, 'razorpay');

   123:     assert.ok(purchase.paidAt, 'paidAt stamped');
   124:     assert.ok(purchase.verifiedAt, 'verifiedAt stamped');
   125: 
>  126:     const createOrUpdate = db.calls.find((c) => c.col === 'course_purchases');
   127:     assert.deepEqual(createOrUpdate.permissions, [
   128:       `read("user:${USER}")`,
   129:       'read("team:admins")',

   133: 
   134:     const attemptUpdate = db.calls.find((c) => c.col === 'payment_attempts' && c.op === 'update' && c.data.status);
   135:     assert.equal(attemptUpdate.data.status, 'verified');
>  136:     const grantIndex = db.calls.findIndex((c) => c.col === 'course_purchases');
   137:     const attemptVerifiedIndex = db.calls.findIndex(
   138:       (c) => c.col === 'payment_attempts' && c.op === 'update' && c.data.status === 'verified',
   139:     );

   143:   test('reuses and verifies an existing pending ledger row instead of creating a duplicate', async () => {
   144:     const db = createInMemoryDb();
   145:     db.seed('payment_attempts', 'att_1', buildAttempt());
>  146:     db.seed('course_purchases', EXPECTED_PURCHASE_ID, {
   147:       userId: USER,
   148:       categoryId: CATEGORY,
   149:       provider: 'razorpay',

   171: 
   172:     assert.equal(stats.reconciled, 1);
   173:     assert.ok(
>  174:       !db.calls.some((c) => c.op === 'create' && c.col === 'course_purchases'),
   175:       'no duplicate ledger row is created',
   176:     );
   177:     assert.equal(db.docs.get(`course_purchases/${EXPECTED_PURCHASE_ID}`).status, 'verified');

   178:   });
   179: 
   180:   test('fails closed on amount mismatch: no grant, attempt flagged amount_mismatch', async () => {

   192:     });
   193: 
   194:     assert.equal(stats.failed, 1);
>  195:     assert.ok(!db.docs.has(`course_purchases/${EXPECTED_PURCHASE_ID}`), 'no ledger row granted');
   196:     const attemptUpdate = db.calls.find((c) => c.col === 'payment_attempts' && c.op === 'update');
   197:     assert.equal(attemptUpdate.data.reconciliationStatus, 'amount_mismatch');
   198:     assert.equal(attemptUpdate.data.status, undefined, 'attempt is not marked verified');

   213:     });
   214: 
   215:     assert.equal(stats.failed, 1);
>  216:     assert.ok(!db.docs.has(`course_purchases/${EXPECTED_PURCHASE_ID}`), 'no ledger row granted');
   217:   });
   218: 
   219:   test('expires attempts whose gateway order is not paid', async () => {

   233:     assert.equal(stats.reconciled, 1);
   234:     const attemptUpdate = db.calls.find((c) => c.col === 'payment_attempts' && c.op === 'update');
   235:     assert.equal(attemptUpdate.data.status, 'expired');
>  236:     assert.ok(!db.docs.has(`course_purchases/${EXPECTED_PURCHASE_ID}`));
   237:   });
   238: 
   239:   test('abandons attempts without a gateway order id', async () => {


===== functions/verifyCoursePurchase/src/main.js =====
    83:       // 1. Fetch pending purchase ledger entry by purchaseId (exact userId:categoryId)
    84:       let pendingPurchase;
    85:       try {
>   86:         pendingPurchase = await databases.getDocument(databaseId, 'course_purchases', purchaseId);
    87:       } catch (err) {
    88:         if (err.code === 404) {
    89:           return res.json({ ok: false, message: 'No pending purchase order found for this category. Please initiate order first.' }, 404);

   249: 
   250:       const purchase = await databases.updateDocument(
   251:         databaseId,
>  252:         'course_purchases',
   253:         purchaseId,
   254:         verifiedLedger,
   255:         documentPermissions


===== functions/verifyCoursePurchase/src/shared/payment_state.js =====
    97: }
    98: 
    99: // Request-scoped adapter: preserve each handler's gateway/signature/claim logic,
>  100: // but require every course_purchases update to pass the same transaction gate.
   101: // Reads retain order/payment binding so a late refund cannot affect a repurchase.
   102: export function withPaymentStateGuard(databases, { event = 'capture' } = {}) {
   103:   if (databases?.__isPaymentStateGuarded && databases.__guardedEvent === event) {

   118:       if (prop === 'getDocument') return async (...args) => {
   119:         const col = typeof args[0] === 'object' ? args[0].collectionId : args[1];
   120:         const doc = await target.getDocument(...args);
>  121:         if (col === 'course_purchases') {
   122:           if (capture) assertCaptureAllowed(doc);
   123:           remember(doc);
   124:         }

   127:       if (prop === 'listDocuments') return async (...args) => {
   128:         const col = typeof args[0] === 'object' ? args[0].collectionId : args[1];
   129:         const result = await target.listDocuments(...args);
>  130:         if (col === 'course_purchases') result.documents.forEach(remember);
   131:         return result;
   132:       };
   133:       if (prop === 'createDocument') return async (...args) => {

   134:         const col = typeof args[0] === 'object' ? args[0].collectionId : args[1];
   135:         const data = typeof args[0] === 'object' ? args[0].data : args[3];
>  136:         if (col === 'course_purchases' && data?.status === 'verified') {
   137:           throw new PaymentStateConflict('Missing purchase ledger requires manual reconciliation.');
   138:         }
   139:         return target.createDocument(...args);

   143:           databaseId: args[0], collectionId: args[1], documentId: args[2],
   144:           data: args[3], permissions: args[4],
   145:         };
>  146:         if (options.collectionId !== 'course_purchases') return target.updateDocument(...args);
   147:         if (typeof target.createTransaction !== 'function' || typeof target.updateTransaction !== 'function') {
   148:           throw Object.assign(new Error('Payment transactions are unavailable.'), { code: 503 });
   149:         }


===== lib/app/router/route_names.dart =====
     4:   static const login = 'login';
     5:   static const home = 'home';
     6:   static const categories = 'categories';
>    7:   static const lessons = 'lessons';
     8:   static const lessonDetail = 'lessonDetail';
     9:   static const quiz = 'quiz';
    10:   static const profile = 'profile';


===== lib/core/constants/app_constants.dart =====
    65:   static const String colCategories = 'categories';
    66:   static const String colFeaturedBanners = 'featuredBanners';
    67:   static const String colLetters = 'letters';
>   68:   static const String colLessons = 'lessons';
    69:   static const String colQuizzes = 'quizzes';
    70:   static const String colUsers = 'users';
    71:   static const String colProgress = 'progress';


===== lib/core/payments/purchase_repository.dart =====
   126:     try {
   127:       final db = ref.read(appwriteDbServiceProvider);
   128:       final result = await db.listDocuments(
>  129:         'course_purchases',
   130:         queries: [
   131:           appwrite.Query.equal('userId', userId),
   132:           appwrite.Query.equal('status', 'verified'),


===== lib/core/reviews/review_eligibility.dart =====
    23:   try {
    24:     final db = ref.read(appwriteDbServiceProvider);
    25:     final result = await db.listDocuments(
>   26:       'course_purchases',
    27:       queries: [
    28:         Query.equal('userId', user.id),
    29:         Query.equal('unlockMethod', 'play_store_review'),


===== lib/features/admin/domain/content_badge_resolver.dart =====
    76:       // TODO: If ContentBadgeType.rhyme is ever needed as a distinct visual,
    77:       // map it here. For now, rhymes are audio play-along, so they map to audio.
    78:       return ContentBadgeType.audio;
>   79:     case ContentKind.lesson:
    80:       if (isLettersCat || isNumbersCat) {
    81:         return ContentBadgeType
    82:             .tracing; // Lessons on letters/numbers are tracing exercises


===== lib/features/admin/presentation/content/admin_content_list_screen.dart =====
    45:   bool get _supportsCategory =>
    46:       widget.kind == ContentKind.word ||
    47:       widget.kind == ContentKind.sentence ||
>   48:       widget.kind == ContentKind.lesson ||
    49:       widget.kind == ContentKind.rhyme;
    50:   bool get _supportsPublished => widget.kind != ContentKind.rhyme;
    51:   bool get _supportsPremium =>

>   52:       widget.kind == ContentKind.lesson || widget.kind == ContentKind.rhyme;
    53:   bool get _supportsTags => widget.kind == ContentKind.rhyme;
    54: 
    55:   @override

    90:         return 'Vocabulary Words';
    91:       case ContentKind.sentence:
    92:         return 'Sentences & Phrases';
>   93:       case ContentKind.lesson:
    94:         return 'Subcategories';
    95:       case ContentKind.rhyme:
    96:         return 'Rhymes & Stories';

   107:         return 'CONTENT · WORDS';
   108:       case ContentKind.sentence:
   109:         return 'CONTENT · SENTENCES';
>  110:       case ContentKind.lesson:
   111:         return 'CONTENT · SUBCATEGORIES';
   112:       case ContentKind.rhyme:
   113:         return 'CONTENT · RHYMES';

   124:         return 'Manage words and their meanings';
   125:       case ContentKind.sentence:
   126:         return 'Manage phrases and conversations';
>  127:       case ContentKind.lesson:
   128:         return 'Create and manage subcategories';
   129:       case ContentKind.rhyme:
   130:         return 'Manage kid-friendly music and stories';

   141:         return Icons.menu_book_rounded;
   142:       case ContentKind.sentence:
   143:         return Icons.format_quote_rounded;
>  144:       case ContentKind.lesson:
   145:         return Icons.school_rounded;
   146:       case ContentKind.rhyme:
   147:         return Icons.music_note_rounded;

   212:     final isDark = Theme.of(context).brightness == Brightness.dark;
   213:     final isWideScreen = MediaQuery.of(context).size.width > 800;
   214: 
>  215:     if (widget.kind == ContentKind.lesson &&
   216:         (_selectedCategoryId == null || _selectedCategoryId!.isEmpty) &&
   217:         categories.isNotEmpty) {
   218:       WidgetsBinding.instance.addPostFrameCallback((_) {

   435:                       kind: widget.kind,
   436:                       item: item,
   437:                     ),
>  438:                     onEditBlocks: widget.kind == ContentKind.lesson
   439:                         ? (item) =>
   440:                               context.go('/admin/lessons/content/${item.id}')
   441:                         : null,


===== lib/features/admin/presentation/content/utils/content_csv_exporter.dart =====
     6: String buildContentListCsv(ContentKind kind, List<ContentItem> items) {
     7:   final isSentence = kind == ContentKind.sentence;
     8:   final isLessonOrRhyme =
>    9:       kind == ContentKind.lesson || kind == ContentKind.rhyme;
    10: 
    11:   final String csvHeader;
    12:   if (isSentence) {


===== lib/features/admin/presentation/content/utils/content_list_actions.dart =====
    25:       case ContentKind.sentence:
    26:         ref.invalidate(sentencesProvider);
    27:         break;
>   28:       case ContentKind.lesson:
    29:         ref.invalidate(lessonNotifierProvider);
    30:         break;
    31:       case ContentKind.rhyme:


===== lib/features/admin/presentation/lessons/admin_lessons_screen.dart =====
     9:   @override
    10:   Widget build(BuildContext context) {
    11:     return AdminContentListScreen(
>   12:       kind: ContentKind.lesson,
    13:       categoryId: categoryId,
    14:     );
    15:   }


===== lib/features/admin/presentation/lessons/content/admin_lesson_content_screen.dart =====
   100:   Future<void> _loadData() async {
   101:     try {
   102:       final repo = ref.read(contentRepositoryProvider);
>  103:       final result = await repo.get(ContentKind.lesson, widget.lessonId);
   104:       result.fold(
   105:         (failure) {
   106:           if (mounted) context.go('/admin/lessons');

   177:           _lesson = savedItem.toLessonEntity();
   178:           _blocks = List.from(_lesson!.blocks);
   179: 
>  180:           ref.invalidate(contentListProvider((ContentKind.lesson, null)));
   181:           if (_lesson != null && _lesson!.categoryId.isNotEmpty) {
   182:             ref.invalidate(
   183:               contentListProvider((ContentKind.lesson, _lesson!.categoryId)),

   184:             );
   185:           }
   186:           ref.invalidate(

>  187:             contentDetailProvider((ContentKind.lesson, widget.lessonId)),
   188:           );
   189:           // ignore: deprecated_member_use
   190:           ref.invalidate(lessonNotifierProvider);


===== lib/features/admin/presentation/lessons/content/widgets/lesson_block_card.dart =====
   123:               children: [
   124:                 ContentTypeBadge(
   125:                   type: resolveBadgeType(
>  126:                     kind: ContentKind.lesson,
   127:                     categoryId: categoryId,
   128:                     categorySlug: categorySlug,
   129:                     blockType: block.type,


===== lib/features/admin/presentation/lessons/widgets/lesson_form_sheet.dart =====
    47:     });
    48: 
    49:     final repo = ref.read(contentRepositoryProvider);
>   50:     final res = await repo.get(ContentKind.lesson, l.id);
    51: 
    52:     res.fold(
    53:       (failure) {

    88: 
    89:     _initialItem = ContentItem(
    90:       id: l.id,
>   91:       kind: ContentKind.lesson,
    92:       categoryId: l.categoryId,
    93:       title: l.titleLatin,
    94:       titleOlChiki: l.titleOlChiki,

   152:             child: _isLoading
   153:                 ? const Center(child: CircularProgressIndicator())
   154:                 : ContentForm(
>  155:                     kind: ContentKind.lesson,
   156:                     categoryId: widget.initialCategoryId,
   157:                     initial: _initialItem,
   158:                     onSubmit: _onSubmit,


===== lib/features/admin/presentation/translations/widgets/translation_edit_dialog_inputs.dart =====
   284:   if (blockMatch != null) {
   285:     final lessonId = blockMatch.group(1)!;
   286:     final blockIdx = int.tryParse(blockMatch.group(2)!) ?? 0;
>  287:     final res = await repo.get(ContentKind.lesson, lessonId);
   288:     await res.fold((_) async {}, (item) async {
   289:       final lesson = item.toLessonEntity();
   290:       if (blockIdx < lesson.blocks.length) {

   324:         await repo.upsert(updatedItem);
   325:       }
   326:     });
>  327:     ref.invalidate(contentListProvider((ContentKind.lesson, null)));
   328:     ref.invalidate(lessonNotifierProvider);
   329:   } else if (entry.kind == TranslationKind.lesson) {
   330:     final res = await repo.get(ContentKind.lesson, entry.id);

   331:     await res.fold((_) async {}, (item) async {
   332:       final updatedItem = item.copyWith(
   333:         subtitle: enMeaning.isNotEmpty ? enMeaning : item.subtitle,

   335:       );
   336:       await repo.upsert(updatedItem);
   337:     });
>  338:     ref.invalidate(contentListProvider((ContentKind.lesson, null)));
   339:     ref.invalidate(lessonNotifierProvider);
   340:   } else if (entry.kind == TranslationKind.word) {
   341:     final res = await repo.get(ContentKind.word, entry.id);


===== lib/features/admin/presentation/widgets/content_form/content_form_identity_section.dart =====
    51:       kind != ContentKind.letter && kind != ContentKind.number;
    52:   bool get _supportsPublished => kind != ContentKind.rhyme;
    53:   bool get _supportsPremium =>
>   54:       kind == ContentKind.lesson || kind == ContentKind.rhyme;
    55:   bool get _supportsSubtitle => kind != ContentKind.number;
    56:   bool get _supportsOrder => kind != ContentKind.rhyme;
    57:   bool get _supportsTags => kind == ContentKind.rhyme;

    93:               ),
    94:               const SizedBox(height: 12),
    95: 
>   96:               if (kind == ContentKind.lesson) ...[
    97:                 ExpansionTile(
    98:                   title: const Text(
    99:                     'Advanced Titles (Ol Chiki, Glyphs)',


===== lib/features/admin/presentation/widgets/content_form.dart =====
   253:             checks: const [
   254:               ReferenceCheck(
   255:                 databaseId: 'olitun_db',
>  256:                 collectionId: 'lessons',
   257:                 fieldNames: ['thumbnailUrl', 'heroMediaUrl'],
   258:               ),
   259:               ReferenceCheck(

   375:               title: 'Hero Cover Media',
   376:               child: MediaPickerField(
   377:                 label: 'Cover Visual Element',
>  378:                 kind: widget.kind == ContentKind.lesson
   379:                     ? ContentMediaKind.video
   380:                     : ContentMediaKind.image,
   381:                 value: _heroMedia,


===== lib/features/home/presentation/home_screen.dart =====
   115:   Future<void> _onRefresh() async {
   116:     await ref.read(homePrefetchProvider.notifier).prefetch(forceRefresh: true);
   117:     if (!mounted) return;
>  118:     ref.invalidate(contentListProvider((ContentKind.lesson, null)));
   119:     ref.invalidate(featuredBannersProvider);
   120:   }
   121: 

   196:                         buildContentLoadGuard(
   197:                           [lessonsAsync],
   198:                           onRetry: () => ref.invalidate(
>  199:                             contentListProvider((ContentKind.lesson, null)),
   200:                           ),
   201:                         ) ??
   202:                         NextBestActionCard(nextLessonId: nextLesson?.id),


===== lib/features/lessons/data/datasources/lesson_remote_datasource.dart =====
    61:             result.statusCode != 200 ||
    62:             data == null ||
    63:             data['ok'] != true ||
>   64:             data['lessons'] is! List) {
    65:           throw ServerException(
    66:             message:
    67:                 data?['message'] as String? ??

    70:           );
    71:         }
    72: 
>   73:         for (final rawLesson in data['lessons'] as List) {
    74:           if (rawLesson is! Map) {
    75:             throw ServerException(
    76:               message: 'Malformed authorized lesson metadata',

   219:       await databases
   220:           .createDocument(
   221:             databaseId: AppwriteConfig.databaseId,
>  222:             collectionId: 'lessons',
   223:             documentId: lesson.id,
   224:             data: data,
   225:             permissions: _readPermissions(decision),

   249:       await databases
   250:           .updateDocument(
   251:             databaseId: AppwriteConfig.databaseId,
>  252:             collectionId: 'lessons',
   253:             documentId: lesson.id,
   254:             data: data,
   255:             permissions: _readPermissions(decision),

   271:       await databases
   272:           .deleteDocument(
   273:             databaseId: AppwriteConfig.databaseId,
>  274:             collectionId: 'lessons',
   275:             documentId: id,
   276:           )
   277:           .timeout(_writeTimeout);


===== lib/features/lessons/presentation/category_lessons_screen.dart =====
    38:   Future<void> _onRefresh() async {
    39:     ref.invalidate(categoryNotifierProvider);
    40:     ref.invalidate(
>   41:       contentListProvider((ContentKind.lesson, widget.categoryId)),
    42:     );
    43:     ref.invalidate(contentListProvider((ContentKind.lesson, null)));
    44:   }

    45: 
    46:   void _backToLearningPaths() {

   437:                         onPressed: () {
   438:                           ref.invalidate(
   439:                             contentListProvider((
>  440:                               ContentKind.lesson,
   441:                               widget.categoryId,
   442:                             )),
   443:                           );

   444:                           ref.invalidate(
>  445:                             contentListProvider((ContentKind.lesson, null)),
   446:                           );
   447:                         },
   448:                         icon: const Icon(Icons.refresh_rounded),


===== lib/features/lessons/presentation/lessons_screen.dart =====
    25:   Future<void> _refresh() async {
    26:     await ref.read(categoryNotifierProvider.notifier).refresh();
    27:     if (mounted) {
>   28:       ref.invalidate(contentListProvider((ContentKind.lesson, null)));
    29:     }
    30:   }
    31: 


===== lib/features/lessons/presentation/providers/lesson_notifier.dart =====
    16: final lessonsByCategoryProvider =
    17:     Provider.family<AsyncValue<List<LessonEntity>>, String>((ref, categoryId) {
    18:       final lessonsAsync = ref.watch(
>   19:         contentListProvider((ContentKind.lesson, categoryId)),
    20:       );
    21:       return lessonsAsync.when(
    22:         data: (items) => AsyncValue.data(


===== lib/features/lessons/presentation/widgets/lesson_content/letter_grid_content.dart =====
    31:       [contentAsync, lessonsAsync],
    32:       onRetry: () {
    33:         ref.invalidate(contentListProvider((ContentKind.letter, null)));
>   34:         ref.invalidate(contentListProvider((ContentKind.lesson, null)));
    35:       },
    36:     );
    37:     if (loadState != null) return loadState;


===== lib/features/lessons/presentation/widgets/lesson_content/number_grid_content.dart =====
    30:       [contentAsync, lessonsAsync],
    31:       onRetry: () {
    32:         ref.invalidate(contentListProvider((ContentKind.number, null)));
>   33:         ref.invalidate(contentListProvider((ContentKind.lesson, null)));
    34:       },
    35:     );
    36:     if (loadState != null) return loadState;


===== lib/features/lessons/presentation/widgets/lesson_content/sentence_list_content.dart =====
    30:       [contentAsync, lessonsAsync],
    31:       onRetry: () {
    32:         ref.invalidate(contentListProvider((ContentKind.sentence, null)));
>   33:         ref.invalidate(contentListProvider((ContentKind.lesson, null)));
    34:       },
    35:     );
    36:     if (loadState != null) return loadState;


===== lib/features/lessons/presentation/widgets/lesson_content/vocabulary_list_content.dart =====
    30:       [contentAsync, lessonsAsync],
    31:       onRetry: () {
    32:         ref.invalidate(contentListProvider((ContentKind.word, null)));
>   33:         ref.invalidate(contentListProvider((ContentKind.lesson, null)));
    34:       },
    35:     );
    36:     if (loadState != null) return loadState;


===== lib/shared/models/content_item/content_item_model.dart =====
   146:   /// writes here so a future call site cannot accidentally publish paid bodies.
   147:   /// Lessons must use the category-aware lesson publisher.
   148:   Map<String, dynamic> toAppwrite() {
>  149:     if (kind == ContentKind.lesson) {
   150:       throw StateError(
   151:         'Lesson publication requires the category-aware lesson publisher.',
   152:       );


===== lib/shared/models/content_item/content_item_serialization.dart =====
   215:     var resolvedOlChiki = firstString([json['ol_chiki'], json['olChiki']]);
   216: 
   217:     switch (parsedKind) {
>  218:       case ContentKind.lesson:
   219:         resolvedTitle =
   220:             firstString([json['title'], json['titleLatin']]) ?? resolvedTitle;
   221:         resolvedSubtitle =

   274:     if (parsedHeroMedia == null) {
   275:       String? legacyMediaUrl;
   276:       ContentMediaKind? legacyMediaKind;
>  277:       if (parsedKind == ContentKind.lesson) {
   278:         legacyMediaUrl = firstString([
   279:           json['heroMediaUrl'],
   280:           json['thumbnailUrl'],

   365: 
   366:   static Map<String, dynamic> toAppwrite(ContentItem item) {
   367:     final requiresCategory =
>  368:         item.kind == ContentKind.lesson ||
   369:         item.kind == ContentKind.word ||
   370:         item.kind == ContentKind.sentence ||
   371:         item.kind == ContentKind.rhyme;

   407:     }
   408: 
   409:     switch (item.kind) {
>  410:       case ContentKind.lesson:
   411:         return {
   412:           if (item.categoryId.isNotEmpty) 'categoryId': item.categoryId,
   413:           'titleOlChiki': resolvedTitleOlChiki,


===== lib/shared/providers/dashboard_metrics_provider.dart =====
    91: 
    92: final List<_CollectionSpec> _activitySpecs = [
    93:   _CollectionSpec(
>   94:     id: 'lessons',
    95:     singular: 'Lesson',
    96:     icon: Icons.school_rounded,
    97:     color: AppColors.brandBlue,

   256:     return buckets;
   257:   }
   258: 
>  259:   final lessonsSeries = bucketCreated('lessons');
   260:   final vocabularySeries = bucketCreated('words');
   261: 
   262:   // ── Week-over-week delta ───────────────────────────────────────────────


===== lib/shared/providers/learner_content_providers.dart =====
     6: 
     7: final learnerLessonsProvider = Provider<AsyncValue<List<LessonEntity>>>((ref) {
     8:   return ref
>    9:       .watch(contentListProvider((ContentKind.lesson, null)))
    10:       .whenData(
    11:         (list) => list
    12:             .map((item) => scopeLessonMedia(item.toLessonEntity()))


===== lib/shared/providers/purchases_provider.dart =====
   251:       }
   252: 
   253:       final result = await db.listDocuments(
>  254:         'course_purchases',
   255:         queries: queries,
   256:       );
   257: 

   318:       }
   319: 
   320:       final result = await db.listDocuments(
>  321:         'course_purchases',
   322:         queries: queries,
   323:       );
   324: 

   501: 
   502:       while (attempts < maxRetries && result == null) {
   503:         try {
>  504:           result = await db.listDocuments('course_purchases', queries: queries);
   505:         } catch (e) {
   506:           attempts++;
   507:           if (attempts >= maxRetries) {


===== lib/shared/providers/seed_provider.dart =====
    52: 
    53:   final lessonRows = await ref
    54:       .read(appwriteDbServiceProvider)
>   55:       .listDocuments('lessons');
    56:   final existingLessonIds = lessonRows
    57:       .map((row) => row['id'] as String)
    58:       .toSet();


===== lib/shared/repositories/content_repository.dart =====
    61:     ContentKind.number => 'numbers',
    62:     ContentKind.word => 'words',
    63:     ContentKind.sentence => 'sentences',
>   64:     ContentKind.lesson => 'lessons',
    65:     ContentKind.rhyme => 'rhymes',
    66:   };
    67: 

    68:   String? _categoryAttribute(ContentKind kind) => switch (kind) {
>   69:     ContentKind.lesson || ContentKind.rhyme => 'categoryId',
    70:     ContentKind.word || ContentKind.sentence => 'category',
    71:     ContentKind.letter || ContentKind.number => null,
    72:   };

   249:   }
   250: 
   251:   Future<PublicationDecision> _publicationDecisionFor(ContentItem item) async {
>  252:     if (item.kind != ContentKind.lesson) {
   253:       if (item.isPremium) {
   254:         return const PublicationDecision.protected('item-marked-premium');
   255:       }


===== lib/shared/repositories/content_seed_loader.dart =====
    24:         // category filters for letters and numbers.
    25:         return await _loadBundledBasics(kind);
    26:       }
>   27:       if (kind == ContentKind.lesson) {
    28:         final List<ContentItem> allLessons = [];
    29: 
    30:         // 1. Sentence & Grammar & Folktale Lessons (23 lessons)

    40:                   final lesson = LessonModel.fromJson(map);
    41:                   return ContentItem(
    42:                     id: lesson.id,
>   43:                     kind: ContentKind.lesson,
    44:                     categoryId: lesson.categoryId.isNotEmpty
    45:                         ? lesson.categoryId
    46:                         : 'cat_sentences',

    80:               final lesson = LessonModel.fromJson(map);
    81:               return ContentItem(
    82:                 id: lesson.id,
>   83:                 kind: ContentKind.lesson,
    84:                 categoryId: lesson.categoryId.isNotEmpty
    85:                     ? lesson.categoryId
    86:                     : 'cat_vocab',

   102:             }).toList();
   103:           } catch (e, stack) {
   104:             _cachedBundledVocabLessons = [];
>  105:             _logSeedLoadFailure('lessons', e, stack);
   106:           }
   107:         }
   108: 


===== scripts/appwrite_import.mjs =====
   222:   // Import order matters (foreign keys)
   223:   const importOrder = [
   224:     'categories',
>  225:     'lessons',
   226:     'lesson_blocks',
   227:     'letters',
   228:     'numbers',


===== scripts/appwrite_seed.mjs =====
   306:   }));
   307: 
   308:   if (allLessons.length > 0) {
>  309:     await importCollection('lessons', allLessons);
   310:   }
   311: 
   312:   console.log('\n🎉 Seed import complete!');


===== scripts/appwrite_setup.mjs =====
    71:   'translation_cache',
    72:   'rate_limits',
    73:   'reward_events',
>   74:   'course_purchases',
    75:   'payment_claims',
    76:   'refund_claims',
    77:   'payment_attempts',

   171:     ],
   172:   },
   173:   {
>  174:     id: 'lessons',
   175:     name: 'Lessons',
   176:     attrs: [
   177:       { type: 'string', key: 'categoryId', size: 36, required: true },

   750:     ],
   751:   },
   752:   {
>  753:     id: 'course_purchases',
   754:     name: 'Course Purchases',
   755:     documentSecurity: true,
   756:     attrs: [


===== scripts/check_premium_content_permissions.mjs =====
   197: 
   198:   const [categories, lessons] = await Promise.all([
   199:     listDocuments(config, 'categories'),
>  200:     listDocuments(config, 'lessons'),
   201:   ]);
   202:   const byId = new Map(categories.map((category) => [category.$id, category]));
   203:   let drift = 0;


===== scripts/cleanup_orphaned_media.mjs =====
   187:       fieldPaths: ['audioFileId']
   188:     },
   189:     {
>  190:       collectionId: 'lessons',
   191:       fieldPaths: ['blocks', 'hero_media']
   192:     },
   193:     {


===== scripts/fix_permissions.mjs =====
    39: 
    40: const collectionsToFix = [
    41:   'categories',
>   42:   'lessons',
    43:   'quizzes',
    44:   'letters',
    45:   'numbers',


===== scripts/generate_audio_bodhan_alphanum.mjs =====
    21:  *   BODHAN_API_KEY="sk-..." node scripts/generate_audio_bodhan_alphanum.mjs
    22:  *
    23:  * ENV:
>   24:  *   TARGET="all" | "letters" | "numbers" | "lessons"
    25:  *   CREATE_MISSING="1" (upsert n_10..n_100 + lessons 21-50/51-100; default 1)
    26:  *   WRITE_SEEDS="1" (patch assets/seed/letters.json + numbers.json audioUrl
    27:  *     for succeeded docs; default 0 — run after generation completes)

   231:   // (writes nothing in LIST_ONLY / DRY_RUN modes)
   232:   let numbersCatId = 'cat_numbers';
   233:   const mayWrite = !LIST_ONLY && !DRY_RUN;
>  234:   if (mayWrite && CREATE_MISSING && (TARGET === 'all' || TARGET === 'numbers' || TARGET === 'lessons')) {
   235:     console.log('\n📥 Ensuring numbers n_10..n_100 exist...');
   236:     let created = 0;
   237:     for (let v = 10; v <= 100; v++) {

   247: 
   248:     // Resolve the live numbers category from the existing 10-20 lesson so the
   249:     // new lessons land in the same category the app actually displays.
>  250:     const ref = await getDocument('lessons', 'lesson_numbers_10_20');
   251:     if (ref) numbersCatId = ref.categoryId || numbersCatId;
   252:     console.log(`   numbers category: ${numbersCatId}`);
   253:     for (const [id, start, end, order] of [['lesson_numbers_21_50', 21, 50, 2], ['lesson_numbers_51_100', 51, 100, 3]]) {

>  254:       const exists = await getDocument('lessons', id);
   255:       if (exists) { console.log(`   ${id} exists (${parseBlocks(exists).length} blocks)`); continue; }
   256:       const blocks = [];
   257:       for (let v = start; v <= end; v++) {

   258:         blocks.push({ type: 'text', textOlChiki: _olNumeral(v), textLatin: `${v} – ${_english(v)}` });
   259:       }
>  260:       await createDocument('lessons', id, {
   261:         categoryId: numbersCatId,
   262:         titleOlChiki: `${_olNumeral(start)}-${_olNumeral(end)} ᱮᱞᱠᱷᱟ`,
   263:         titleLatin: `Numbers ${start}-${end}`,

   266:       console.log(`   ${id} created (${blocks.length} blocks)`);
   267:     }
   268:   } else {
>  269:     const ref = await getDocument('lessons', 'lesson_numbers_10_20').catch(() => null);
   270:     if (ref) numbersCatId = ref.categoryId || numbersCatId;
   271:   }
   272: 

   300:     console.log(`   numbers needing audio: ${numberQueue.length}`);
   301:   }
   302: 
>  303:   if (TARGET === 'all' || TARGET === 'lessons') {
   304:     const docs = (await fetchAllDocuments('lessons')).filter(isAlphaNumLesson);
   305:     console.log(`\n📥 alphabet/number lessons: ${docs.length}`);
   306:     // number-name lookup for speaking names instead of bare numerals

   307:     const numDocs = await fetchAllDocuments('numbers');

   400:     if (n % 5 === 0) saveProgress();
   401:   }
   402:   for (const l of lessons) {
>  403:     const res = await getDocument('lessons', l.id);
   404:     if (!res) { console.log(`❌ ${l.id} fetch failed`); failed += l.gaps.length; n += l.gaps.length; continue; }
   405:     const blocks = parseBlocks(res);
   406:     let dirty = false;

   422:     if (dirty && !DRY_RUN) {
   423:       // NOTE: processOne already uploaded the file; just persist block links.
   424:       try {
>  425:         await patchDocument('lessons', l.id, { blocks: JSON.stringify(blocks) });
   426:         console.log(`📝 ${l.id} patched`);
   427:       } catch (e) { console.log(`❌ ${l.id} patch failed: ${e.message}`); }
   428:     }


===== scripts/snapshot_appwrite_schema.mjs =====
    80:   const collections = [
    81:     { id: 'categories', name: 'Categories' },
    82:     { id: 'rhyme_subcategories', name: 'Rhyme Subcategories' },
>   83:     { id: 'lessons', name: 'Lessons' },
    84:     { id: 'quizzes', name: 'Quizzes' },
    85:     { id: 'letters', name: 'Letters' },
    86:     { id: 'numbers', name: 'Numbers' },

   113:     { id: 'bakhed_cultural_notes', name: 'Bakhed Cultural Notes' },
   114:     { id: 'bakhed_listening_progress', name: 'Bakhed Listening Progress' },
   115:     { id: 'daily_affirmations', name: 'Daily Affirmations' },
>  116:     { id: 'course_purchases', name: 'Course Purchases' },
   117:     { id: 'payment_claims', name: 'Payment Claims' },
   118:     { id: 'refund_claims', name: 'Refund Claims' },
   119:     { id: 'binti_guru_waitlist', name: 'Binti Guru Waitlist' }


===== scripts/sync_lessons_and_content_to_appwrite.mjs =====
   123:       categoryId: 'cat_sentences_1778594024495',
   124:       blocks: typeof lesson.blocks === 'object' ? JSON.stringify(lesson.blocks) : (lesson.blocks || '[]'),
   125:     };
>  126:     return upsertDocument(headers, 'lessons', lesson.id, docData);
   127:   });
   128:   console.log(`\n✅ Lessons: ${lRes.created} created, ${lRes.updated} updated, ${lRes.failed} failed.\n`);
   129: 

   144:       categoryId: 'cat_vocab_1778594020532',
   145:       blocks: typeof lesson.blocks === 'object' ? JSON.stringify(lesson.blocks) : (lesson.blocks || '[]'),
   146:     };
>  147:     return upsertDocument(headers, 'lessons', lesson.id, docData);
   148:   });
   149:   console.log(`\n✅ Vocab Lessons: ${vRes.created} created, ${vRes.updated} updated, ${vRes.failed} failed.\n`);
   150: 


